---
phase: quick-260612-o9u
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/bots/BotPerfCard.tsx
  - src/app/bots/performance/BotPerformanceClient.tsx
  - src/app/api/bots/trades/route.ts
  - data/bot-trades-FiFT3HmJf-.json
autonomous: true
requirements: [TODO-BOTPERF]

must_haves:
  truths:
    - "Die Bot-Performance-Karte zeigt nur noch 3 KPIs (Win Rate, P&L, Trades), kein Avg RR mehr."
    - "Das Performance-Fenster aktualisiert sich automatisch alle ~10s ohne in den 'Laedt…'-Zustand zu flackern."
    - "Das Performance-Fenster und die Bots-Uebersicht zeigen identische P&L-Werte (gleiche Datenquelle)."
    - "Die 6 reparierten Trades in data/bot-trades-FiFT3HmJf-.json haben botId/sourceId = kYH5wxoW99."
  artifacts:
    - path: "src/app/api/bots/trades/route.ts"
      provides: "GET-Route, die Bot-attribuierte Profile-Trades liefert"
      exports: ["GET"]
    - path: "src/components/bots/BotPerfCard.tsx"
      provides: "3-KPI-Karte ohne Avg RR"
    - path: "src/app/bots/performance/BotPerformanceClient.tsx"
      provides: "Auto-Refresh + vereinheitlichte P&L-Quelle"
  key_links:
    - from: "src/app/bots/performance/BotPerformanceClient.tsx"
      to: "/api/bots/trades"
      via: "fetch mit profileId"
      pattern: "api/bots/trades"
    - from: "src/app/api/bots/trades/route.ts"
      to: "getProfileTrades"
      via: "Filter t.sourceId"
      pattern: "getProfileTrades"
---

<objective>
Behebt vier verifizierte Bugs im Bot-Performance-Fenster: (1) nutzloses Avg-RR-KPI entfernen, (2) Auto-Refresh ergaenzen, (3) P&L-Quelle mit der Bots-Uebersicht vereinheitlichen, (4) einmalige Datenreparatur von 6 Trades, die durch einen inzwischen gefixten Bug (Commit 0d62169) ihre Bot-Zuordnung verloren haben.

Purpose: Konsistente, sich selbst aktualisierende Bot-Performance-Anzeige mit korrekter Trade-Attribution — ohne korrekte Attribution sind die P&L-Werte (+9,50EUR vs -0,69EUR Divergenz) wertlos.
Output: Geaenderte Karte/Client, neue API-Route, reparierte Datendatei.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@src/components/bots/BotPerfCard.tsx
@src/app/bots/performance/BotPerformanceClient.tsx
@src/app/api/bots/[id]/stats/route.ts
@src/app/api/bridge/trades/route.ts
@src/lib/profiles.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Avg-RR-KPI entfernen + Auto-Refresh ergaenzen</name>
  <files>src/components/bots/BotPerfCard.tsx, src/app/bots/performance/BotPerformanceClient.tsx</files>
  <action>
BotPerfCard.tsx:
- Das KPI-Objekt mit label 'Avg RR' aus dem kpis-Array (aktuell Zeile 64) entfernen.
- Das Grid von grid-cols-4 auf grid-cols-3 aendern (aktuell Zeile 158).
- Im zweiten useMemo (aktuell Zeile 32) die nun ungenutzten Berechnungen entfernen: die Variablen rrSum und rrCount, die Zeile mit `if (t.rr !== undefined) { rrSum += t.rr; rrCount++ }`, sowie die Rueckgabe-Felder avgRR und rrCount. Aus dem Destructuring `const { totalPnl, winRate, avgRR, rrCount, chartData } = useMemo(...)` werden avgRR und rrCount entfernt, sodass nur `{ totalPnl, winRate, chartData }` uebrig bleibt. Sicherstellen, dass keine TS-Unused/Undefined-Referenz zurueckbleibt.

BotPerformanceClient.tsx:
- fetchData (aktuell Zeile 25) so umbauen, dass es KEIN setLoading(true) mehr aufruft (sonst flackert die Seite alle 10s in 'Laedt…'). Den Rumpf von fetchData unveraendert lassen, nur die erste Zeile setLoading(true) und das abschliessende setLoading(false) aus fetchData entfernen.
- Den useEffect (aktuell Zeile 42) ersetzen durch: initialer Load, der fetchData() aufruft und das loading beendet (z.B. `fetchData().finally(() => setLoading(false))`), plus `const interval = setInterval(fetchData, 10000)` und Cleanup `return () => clearInterval(interval)`. Dependency-Array bleibt `[fetchData]`.
- loading wird damit nur einmal beim Erstload true->false; Polling laeuft still im Hintergrund.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>BotPerfCard rendert grid-cols-3 mit 3 KPIs (kein Avg RR, keine avgRR/rrSum/rrCount-Reste); BotPerformanceClient pollt alle 10s via setInterval mit clearInterval-Cleanup und ruft setLoading nur beim Erstload auf; tsc --noEmit ist sauber.</done>
</task>

<task type="auto">
  <name>Task 2: P&L-Quelle vereinheitlichen (neue Route + Client-Umstellung)</name>
  <files>src/app/api/bots/trades/route.ts, src/app/bots/performance/BotPerformanceClient.tsx</files>
  <action>
Neue Datei src/app/api/bots/trades/route.ts anlegen:
- Importe: NextRequest, NextResponse aus 'next/server'; getProfileTrades aus '@/lib/profiles'.
- export async function GET(req: NextRequest): profileId aus req.nextUrl.searchParams.get('profileId') lesen. Wenn fehlt -> NextResponse.json mit { error: 'Missing profileId' } und status 400. Wenn nicht auf /^[a-zA-Z0-9_-]{1,64}$/ passt -> NextResponse.json mit { error: 'Invalid profileId' } und status 400 (identisch zur Validierung in /api/bridge/trades GET).
- Bei gueltiger profileId: NextResponse.json mit { trades: getProfileTrades(profileId).filter(t => t.sourceId) } — nur Trades mit Bot/Bridge-Attribution. Dies nutzt dieselbe Quelle wie /api/bots/[id]/stats (getProfileTrades + sourceId-Filter).

BotPerformanceClient.tsx:
- Den Fetch in fetchData von `/api/bridge/trades?profileId=...` auf `/api/bots/trades?profileId=...` umstellen (aktuell Zeile 29, Template-Literal beibehalten).
- Den Karten-Filter von `t.botId === bw.bot.id` auf `t.sourceId === bw.bot.id` aendern (aktuell Zeile 104), konsistent mit /api/bots/[id]/stats Zeile 17.
- /api/bridge/trades NICHT anfassen (wird von der Bridge per POST zum Sync genutzt).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Datei src/app/api/bots/trades/route.ts exportiert GET mit profileId-Validierung und liefert nur sourceId-Trades; BotPerformanceClient fetcht von /api/bots/trades und filtert Karten per t.sourceId; tsc --noEmit ist sauber.</done>
</task>

<task type="auto">
  <name>Task 3: Einmalige Datenreparatur der 6 Trades</name>
  <files>data/bot-trades-FiFT3HmJf-.json</files>
  <action>
6 geschlossene Trades in data/bot-trades-FiFT3HmJf-.json haben botId: null, sourceId: 'bridge/tradeexecuter' statt korrekt botId: 'kYH5wxoW99', sourceId: 'kYH5wxoW99'. ExternalIds: pos_154884957, pos_154885207, pos_154885363, pos_154885701, pos_154886012, pos_154886835 (verifiziert: alle 6 vorhanden, status closed; Journal-Gegenstuecke in data/trades-FiFT3HmJf-.json haben bereits kYH5wxoW99).

ACHTUNG: Die Bridge laeuft live und schreibt evtl. in diese Datei. Reparatur in EINEM atomaren Schritt durchfuehren (lesen -> patchen -> tmp schreiben -> rename), danach per Re-Read verifizieren.

Inline-One-Off (KEIN Skript im Repo ablegen), via Bash node -e. Format der bestehenden Datei: Top-Level-Array, UTF-8 ohne BOM, 2-Space-Indent — daher JSON.stringify(arr, null, 2) und newline am Dateiende erhalten falls vorhanden. Logik: Datei lesen, JSON.parse, ueber das Array iterieren, fuer jeden Eintrag dessen externalId in der ID-Menge liegt botId='kYH5wxoW99' und sourceId='kYH5wxoW99' setzen, Treffer zaehlen. Wenn Trefferzahl != 6 -> Fehler werfen (nicht blind weiterschreiben). Ergebnis als JSON.stringify(arr,null,2) (plus abschliessendes Newline wenn das Original eines hatte) atomar via tmp-Datei + fs.renameSync zurueckschreiben.

Falls weniger als 6 Treffer (Bridge hat zwischenzeitlich geschrieben), nicht blind nochmal patchen — erneut lesen und pruefen, ob die Eintraege bereits korrekt sind.
  </action>
  <verify>
    <automated>node scripts/_verify_botperf_repair.cjs 2>/dev/null || node -e "const a=require('./data/bot-trades-FiFT3HmJf-.json');const b=require('./data/trades-FiFT3HmJf-.json');const ids=['pos_154884957','pos_154885207','pos_154885363','pos_154885701','pos_154886012','pos_154886835'];const bad=a.filter(t=>ids.includes(t.externalId)&&(t.botId!=='kYH5wxoW99'||t.sourceId!=='kYH5wxoW99'));if(bad.length){throw new Error('Noch falsch: '+bad.length)}const f=arr=>arr.filter(t=>t.status==='closed'&&t.sourceId==='kYH5wxoW99').reduce((s,t)=>s+(t.pnl||0),0);const sa=f(a),sb=f(b);if(Math.abs(sa-sb)>0.01){throw new Error('Summen divergieren: '+sa.toFixed(2)+' vs '+sb.toFixed(2))}console.log('OK bot-trades='+sa.toFixed(2)+' journal='+sb.toFixed(2))"</automated>
  </verify>
  <done>Alle 6 externalIds in data/bot-trades-FiFT3HmJf-.json haben botId/sourceId = kYH5wxoW99; die Summe der geschlossenen kYH5wxoW99-Trades stimmt zwischen bot-trades und trades ueberein (~9,50); der Verify-Check laeuft fehlerfrei durch.</done>
</task>

</tasks>

<verification>
- npx tsc --noEmit laeuft sauber durch (kein TS-Fehler durch entfernte avgRR/rrCount oder neue Route).
- node-Check bestaetigt, dass die Summe der geschlossenen kYH5wxoW99-Trades in data/bot-trades-FiFT3HmJf-.json und data/trades-FiFT3HmJf-.json uebereinstimmt (~9,50).
- Performance-Fenster (/bots/performance) und Bots-Uebersicht (/bots, via /api/bots/[id]/stats) zeigen denselben P&L pro Bot.
</verification>

<success_criteria>
- BotPerfCard zeigt 3 KPIs (grid-cols-3), kein Avg RR.
- BotPerformanceClient pollt alle ~10s mit clearInterval-Cleanup, flackert nicht in 'Laedt…'.
- /api/bots/trades existiert und liefert sourceId-attribuierte Profile-Trades; Client nutzt diese Quelle + t.sourceId-Filter.
- 6 Trades repariert, P&L-Summen konsistent.
</success_criteria>

<commit_notes>
- Bridge laeuft live und schreibt nach data/. Beim Commit NUR die eigenen Dateien explizit stagen (kein git add -A):
  - src/components/bots/BotPerfCard.tsx
  - src/app/bots/performance/BotPerformanceClient.tsx
  - src/app/api/bots/trades/route.ts
  - data/bot-trades-FiFT3HmJf-.json (Live-Daten + Fix zusammen ist ok)
- Die uebrigen dirty data/- und bridge/-Dateien NICHT anfassen.
- Commit-Messages Englisch: fix: fuer Bugfixes, chore: fuer Datenreparatur. Codestil: 2 Spaces, Single Quotes, deutsche UI-Texte.
- Falls beim Verify ein temporaeres Skript scripts/_verify_botperf_repair.cjs angelegt wurde: NICHT committen (loeschen oder ignorieren).
</commit_notes>

<output>
Create `.planning/quick/260612-o9u-bot-performance-fenster-fixen-avg-rr-ent/260612-o9u-SUMMARY.md` when done
</output>
