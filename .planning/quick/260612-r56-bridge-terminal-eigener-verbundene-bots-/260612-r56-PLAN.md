---
phase: 260612-r56
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bridge/gateway.py
  - bridge/main.py
  - bridge/display.py
autonomous: true
requirements: [R56-BOTS-PANEL]

must_haves:
  truths:
    - "Bridge-Terminal zeigt ein eigenes Panel 'Verbundene Bots' zwischen Status-Zeile und Log"
    - "Pro verbundenem Bot eine Zeile mit grünem Punkt, Name, AT-ID, offenen Positionen und Verbindungsdauer"
    - "Bei keinem verbundenen Bot erscheint eine dim-Zeile '(keine Bots verbunden)'"
    - "Die 4. Spalte 'Verbundene Bots' aus der Status-Zeile ist entfernt"
  artifacts:
    - path: bridge/gateway.py
      provides: "get_connected_bots_info() + connected_at-Timestamp im Register-Handler"
      contains: "def get_connected_bots_info"
    - path: bridge/display.py
      provides: "update_bots() + _render_bots_panel() + bots-Layout-Zeile, 3-spaltige Status-Zeile"
      contains: "def _render_bots_panel"
    - path: bridge/main.py
      provides: "Positionszuordnung pro Bot + display.update_bots()-Aufruf"
      contains: "update_bots"
  key_links:
    - from: bridge/main.py
      to: gateway.get_connected_bots_info
      via: "Aufruf an beiden update_status-Stellen"
      pattern: "get_connected_bots_info"
    - from: bridge/main.py
      to: gateway.get_at_bot_id_for_ticket
      via: "Positions-zu-Bot-Matching via Ticket"
      pattern: "get_at_bot_id_for_ticket"
    - from: bridge/main.py
      to: display.update_bots
      via: "Übergabe der bots_info-Liste"
      pattern: "display\\.update_bots"
---

<objective>
Vom Bridge-Terminal (rich-Konsolen-UI in `bridge/display.py`) wird das bisherige
"Verbundene Bots"-Feld (4. Spalte der Status-Zeile) entfernt und durch ein eigenes
Panel zwischen Status-Zeile und Log ersetzt. Pro verbundenem Bot zeigt es eine Zeile:
`● <Name>   ID <AT-ID>   Positionen: <n>   verbunden seit <Dauer>`.

Purpose: Bessere Übersicht über verbundene Bots im Live-Terminal inkl. AT-ID,
Positionszuordnung und Verbindungsdauer — statt einer gequetschten Komma-Liste.
Output: Angepasste `bridge/gateway.py`, `bridge/main.py`, `bridge/display.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

# Verifizierte Code-Stellen (bereits gelesen — Patterns übernehmen):
# bridge/gateway.py:
#   - import-Block (Zeile 1-17): `time` ist NICHT importiert → muss ergänzt werden.
#   - _bot_identities Befüllung im register-Handler (Zeile 350-357).
#   - get_connected_bot_names() (Zeile 137-150) als Vorlage für Name/Fallback-Logik.
#   - _alphatrack_bot_ids (Zeile 209), get_at_bot_id_for_ticket() (Zeile 239-242).
#   - finally-Block entfernt bot_id aus _bots/_bot_identities (Zeile 457-466).
# bridge/main.py:
#   - import aus gateway (Zeile 18) enthält get_connected_bot_names.
#   - update_status-Aufruf #1 (Zeile 310-315, MT5-verloren-Zweig, open_positions=0).
#   - update_status-Aufruf #2 (Zeile 458-467, Haupt-Loop).
#   - mt5.get_open_positions() wird in Zeile 340 nur im if-mt5_ok-Zweig geholt.
# bridge/mt5_connector.py:
#   - get_open_positions() (Zeile 117-139) liefert list[dict] mit 'ticket', [] bei None.
# bridge/display.py:
#   - _render_status_row() (Zeile 131-181): 4. Spalte = Bots (Zeile 172-180).
#   - update_status() Signatur mit connected_bots-Param (Zeile 69-89).
#   - _build_layout() (Zeile 211-218) + Render-Loop log_height (Zeile 238-257).
</context>

<tasks>

<task type="auto">
  <name>Task 1: gateway.py — connected_at-Timestamp + get_connected_bots_info()</name>
  <files>bridge/gateway.py</files>
  <action>
  Ergänze `import time` im import-Block oben (Zeile 1-17; `time` ist aktuell nicht
  importiert). Im register-Handler beim Befüllen von `_bot_identities[bot_id]` (Zeile
  350-357) das Feld `"connected_at": time.time()` hinzufügen — bei Reconnect wird der
  Timestamp bewusst neu gesetzt (akzeptiert).

  Neue Funktion `get_connected_bots_info() -> list[dict]` direkt nach
  `get_connected_bot_names()` (nach Zeile 150) einfügen. Sie iteriert über
  `list(_bots.keys())` (nur tatsächlich per WebSocket verbundene Bots) und liefert pro
  Bot ein dict `{"name": <name>, "at_id": _alphatrack_bot_ids.get(bot_id), "connected_at":
  <ts>}`. Name und connected_at aus `_bot_identities.get(bot_id)`; bei fehlendem Identity-
  Record dieselbe Fallback-Logik wie in `get_connected_bot_names()` (Reverse-Lookup über
  `_bot_names_to_id`) für den Namen, connected_at-Fallback auf `None`. `at_id` bleibt
  `None`, solange die AlphaTrack-Registrierung noch aussteht (Anzeige-Fallback "—" in
  display.py).

  `get_connected_bot_names()` bleibt unverändert bestehen (Rückwärtskompatibilität).
  Deutsche Kommentare wie im Bestand.
  </action>
  <verify>
    <automated>cd bridge && python -m py_compile gateway.py && python -c "import ast,sys; src=open('gateway.py',encoding='utf-8').read(); assert 'def get_connected_bots_info' in src and 'connected_at' in src and 'import time' in src.split('def ')[0], 'fehlt'; print('OK')"</automated>
  </verify>
  <done>`import time` vorhanden; `_bot_identities` enthält `connected_at`; `get_connected_bots_info()` existiert und liefert list[dict] mit name/at_id/connected_at; py_compile fehlerfrei.</done>
</task>

<task type="auto">
  <name>Task 2: display.py — Status-Zeile auf 3 Spalten + Bots-Panel</name>
  <files>bridge/display.py</files>
  <action>
  Status-Zeile bereinigen: In `_render_status_row()` (Zeile 131-181) die 4. Spalte
  ("Verbundene Bots", Zeile 172-180 + die zugehörige 4. `add_column`-Zeile ~137) entfernen
  → nur noch 3 Spalten (AlphaTrack, MT5, Balance). `table.add_row(...)` auf drei Argumente
  reduzieren. In `update_status()` (Zeile 69-89) den Parameter `connected_bots` und dessen
  Zuweisung entfernen; das Attribut `self._connected_bots` (Zeile 53) ebenfalls entfernen.

  Neues Feld `self._bots_info: list[dict] = []` in `__init__` ergänzen (anstelle des
  entfernten `_connected_bots`). Neue Methode `update_bots(self, bots: list[dict]) -> None`,
  die unter `self._lock` `self._bots_info` ersetzt.

  Helper `_format_duration(self, connected_at) -> str`: aus `time.time() - connected_at`
  Sekunden bilden; Format "5s" (<60s), "12m" (<1h), "1h 03m" (>=1h); bei `connected_at`
  None → "—".

  Neue Methode `_render_bots_panel(self) -> Panel`: Titel `[dim]Verbundene Bots[/dim]`,
  border_style="dim", padding=(0, 1). Unter `self._lock` Snapshot von `self._bots_info`
  ziehen. Pro Bot eine `Text`-Zeile: grüner Punkt "● " (style "bold green"), dann
  `<name>` (cyan), "   ID " (dim) + `at_id or "—"`, "   Positionen: " (dim) +
  `<positions>`, "   verbunden seit " (dim) + `_format_duration(connected_at)`. `positions`
  aus `bot.get("positions", 0)`. Bei leerer Liste eine einzelne dim-Zeile
  "(keine Bots verbunden)".

  Layout: In `_build_layout()` (Zeile 211-218) zwischen `status_row` und `log` eine
  Layout-Zeile `Layout(name="bots", size=3)` einfügen. Im Render-Loop (Zeile 235-261)
  Höhe dynamisch setzen: `n = len(self._bots_info); bots_h = max(3, n + 2)` und
  `layout["bots"].size = bots_h`; `log_height` von `terminal_height - 9` auf
  `terminal_height - 9 - bots_h` reduzieren (weiterhin `max(5, ...)`). Im Loop
  `layout["bots"].update(self._render_bots_panel())` ergänzen.

  ASCII-Umlaut-Stil wie im Bestand beibehalten ("fuer", "Verbundene"). Deutsche UI-Texte.
  KEINE fenced code blocks — directive prose; Implementierung folgt den genannten Signaturen.
  </action>
  <verify>
    <automated>cd bridge && python -m py_compile display.py && python -c "from display import BridgeDisplay; import time; d=BridgeDisplay('Test'); d.update_bots([{'name':'TestBot 2','at_id':'kYH5wxoW99','connected_at':time.time()-740,'positions':1}]); p=d._render_bots_panel(); print('OK'); e=BridgeDisplay('Test'); e.update_bots([]); e._render_bots_panel(); print('EMPTY-OK')"</automated>
  </verify>
  <done>Status-Zeile hat 3 Spalten; `connected_bots`-Param und `self._connected_bots` entfernt; `update_bots()`, `_render_bots_panel()`, `_format_duration()` existieren; `bots`-Layout zwischen status_row und log; Smoke-Test (mit Daten + leer) läuft ohne Exception.</done>
</task>

<task type="auto">
  <name>Task 3: main.py — Positionszuordnung + update_bots()-Wiring</name>
  <files>bridge/main.py</files>
  <action>
  Import aus gateway (Zeile 18) anpassen: `get_connected_bot_names` ersetzen durch
  `get_connected_bots_info` und zusätzlich `get_at_bot_id_for_ticket` aufnehmen (mt5 ist
  bereits als Objekt verfügbar, daher kein weiterer Import nötig).

  Helper-Logik (lokale Funktion oder inline an beiden Stellen) zum Aufbau der Bots-Info
  mit Positionszählung: `bots_info = get_connected_bots_info()`; offene Positionen sicher
  holen via `open_pos = mt5.get_open_positions()` (liefert [] bei getrennter MT5 — kein
  zusätzlicher Guard nötig, aber nicht doppelt abfragen). Pro Position via
  `get_at_bot_id_for_ticket(p["ticket"])` die AT-ID auflösen; pro Bot in `bots_info` ein
  Feld `bot["positions"]` setzen = Anzahl Positionen, deren aufgelöste AT-ID gleich
  `bot["at_id"]` ist (Bots mit `at_id` None bekommen `positions` = 0).

  An update_status-Aufruf #1 (Zeile 310-315, MT5-verloren-Zweig): `connected_bots=
  get_connected_bot_names()`-Argument entfernen; davor/danach `bots_info` bauen (hier ist
  MT5 getrennt → leere Positionsliste, positions=0 pro Bot) und
  `display.update_bots(bots_info)` aufrufen.

  An update_status-Aufruf #2 (Zeile 458-467, Haupt-Loop): `connected_bots=...`-Argument
  entfernen; `bots_info` mit Positionszählung bauen und `display.update_bots(bots_info)`
  direkt nach `display.update_status(...)` aufrufen.

  Deutsche Kommentare wie im Bestand. KEINE Prozesse anfassen — Änderungen wirken erst
  nach Bridge-Neustart.
  </action>
  <verify>
    <automated>cd bridge && python -m py_compile main.py && python -c "src=open('main.py',encoding='utf-8').read(); assert 'get_connected_bots_info' in src and 'get_at_bot_id_for_ticket' in src and 'update_bots' in src and 'connected_bots=get_connected_bot_names()' not in src, 'fehlt/Altcode'; print('OK')"</automated>
  </verify>
  <done>Import umgestellt; an beiden update_status-Stellen `connected_bots`-Argument entfernt; `display.update_bots(bots_info)` an beiden Stellen aufgerufen; Positionen pro Bot via get_at_bot_id_for_ticket gematcht; py_compile fehlerfrei.</done>
</task>

</tasks>

<verification>
Gesamt-Compile aller drei Dateien:
`cd bridge && python -m py_compile display.py gateway.py main.py`

Smoke-Test display.py ohne MT5/Live:
BridgeDisplay instanziieren, `update_bots([...])` mit Beispieldaten,
`_render_bots_panel()` aufrufen (kein Live-Start) — wirft keine Exception; ebenso mit
leerer Liste (zeigt "(keine Bots verbunden)").
</verification>

<success_criteria>
- Bridge-Terminal zeigt nach Neustart ein "Verbundene Bots"-Panel zwischen Status-Zeile und Log.
- Pro Bot: grüner Punkt, Name, AT-ID (oder "—"), `Positionen: <n>`, `verbunden seit <Dauer>`.
- Keine Bots → dim-Zeile "(keine Bots verbunden)".
- 4. Status-Spalte entfernt; `connected_bots`-Parameter aus update_status entfernt.
- `python -m py_compile display.py gateway.py main.py` fehlerfrei.
- Commit: nur die drei geänderten bridge-Dateien explizit gestaged (KEIN git add -A); data/-, bridge_log-, ticket_registry-Dateien bleiben unangetastet. Commit-Message Englisch (feat:).
</success_criteria>

<output>
Create `.planning/quick/260612-r56-bridge-terminal-eigener-verbundene-bots-/260612-r56-SUMMARY.md` when done
</output>
