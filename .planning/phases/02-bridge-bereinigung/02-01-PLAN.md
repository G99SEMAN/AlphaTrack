---
phase: 02-bridge-bereinigung
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/context/BotStatusContext.tsx
  - src/app/bridge/BridgeClient.tsx
autonomous: true
requirements: [BRIDGE-01, BRIDGE-02]
must_haves:
  truths:
    - "Eine Bridge, deren lastHeartbeat aelter als 30 Sekunden ist, verschwindet automatisch aus der Bridge-Liste (BRIDGE-01, D-01)"
    - "Eine Bridge ohne status (status === null, noch kein Heartbeat) wird nicht angezeigt (D-01/D-03, Pitfall 3)"
    - "In der Bridge-Tab-Leiste ist kein Trash-Icon / Loeschen-Button mehr sichtbar (BRIDGE-02, D-04)"
    - "npm run build kompiliert ohne TypeScript-Fehler"
  artifacts:
    - path: "src/context/BotStatusContext.tsx"
      provides: "Timeout-Filter in poll() — Bridges mit altem/fehlendem Heartbeat werden vor setBots herausgefiltert"
      contains: "HEARTBEAT_TIMEOUT_MS"
    - path: "src/app/bridge/BridgeClient.tsx"
      provides: "Bridge-Tab-Leiste ohne Loeschen-Button und ohne deleteBot()"
  key_links:
    - from: "src/context/BotStatusContext.tsx poll()"
      to: "setBots"
      via: "gefiltertes next-Array (lastHeartbeat-Check)"
      pattern: "HEARTBEAT_TIMEOUT_MS"
---

<objective>
Die Bridge verwaltet sich selbst: Sie erscheint automatisch beim Verbinden und verschwindet ohne manuellen Eingriff, sobald ihr Heartbeat aelter als 30 Sekunden ist. Gleichzeitig wird der manuelle Loeschen-Button (Trash-Icon) aus der Bridge-UI entfernt, da er nicht mehr gebraucht wird.

Purpose: Erfuellt BRIDGE-01 (Auto-Discovery) und BRIDGE-02 (Trash-Icon entfernt). Beide Eingriffe betreffen die aktive Bridge-Anzeige und sind die Grundlage einer selbstverwaltenden Bridge-Komponente.
Output: Timeout-Filter im BotStatusContext + bereinigte Bridge-Tab-Leiste ohne Loeschen-Funktion.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-bridge-bereinigung/02-CONTEXT.md
@.planning/phases/02-bridge-bereinigung/02-RESEARCH.md
@.planning/phases/02-bridge-bereinigung/02-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Symbole, die in diesem Plan NEU erstellt werden (von der Drift-Verifikation auszuschliessen):
- `HEARTBEAT_TIMEOUT_MS` — Modulkonstante (Wert `30_000`) in `src/context/BotStatusContext.tsx`

Symbole, die in diesem Plan ENTFERNT werden:
- `deleteBot` — Funktion in `src/app/bridge/BridgeClient.tsx`
- `Trash2`-Import in `src/app/bridge/BridgeClient.tsx`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Heartbeat-Timeout-Filter im BotStatusContext (BRIDGE-01)</name>
  <files>src/context/BotStatusContext.tsx</files>
  <read_first>
    - src/context/BotStatusContext.tsx (vollstaendig — aktueller poll()-Stand, Zeilen 29-38; fingerprint() Zeilen 18-22)
    - .planning/phases/02-bridge-bereinigung/02-PATTERNS.md (Abschnitt "BotStatusContext.tsx — Timeout-Filter", Zeilen 25-69)
    - .planning/phases/02-bridge-bereinigung/02-RESEARCH.md (Pattern 1, Pitfall 3 zu status === null)
    - src/types/bot.ts (BotWithStatus.status ist BotStatusWithConnection | null; status.lastHeartbeat)
  </read_first>
  <action>
    Modulkonstante `HEARTBEAT_TIMEOUT_MS = 30_000` direkt nach der `fingerprint`-Funktion (nach Zeile 22), ausserhalb der Komponente, einfuegen — analog zur Modulkonstante `filterBridge` in BridgeClient.tsx Zeile 22.

    In `poll()` (Zeilen 29-38) das Roh-Array aus der API in `raw` aufnehmen (`const raw: BotWithStatus[] = data.bots ?? []`) und vor `setBots` filtern: Mit `const now = Date.now()` ein `next`-Array bilden, das nur Bots enthaelt, deren `b.status?.lastHeartbeat` vorhanden ist UND `now - new Date(b.status.lastHeartbeat).getTime() <= HEARTBEAT_TIMEOUT_MS` erfuellt. Ein Bot ohne `status` bzw. ohne `lastHeartbeat` (`undefined`) wird per `return false` herausgefiltert (entspricht D-01/D-03 und vermeidet Pitfall 3: `new Date(undefined)` ergaebe NaN). Der optionale Chaining-Operator `?.` ist zwingend, da `status` `BotStatusWithConnection | null` ist.

    Die bestehende Zeile `setBots(prev => fingerprint(prev) === fingerprint(next) ? prev : next)` bleibt unveraendert — der Filter wirkt auf `next` BEVOR der fingerprint-Vergleich greift. `setLastUpdated(new Date())` und der `catch`-Block bleiben unveraendert. Das 5s-Poll-Interval (Zeile 44) und die visibilitychange-Logik bleiben unangetastet.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/context/BotStatusContext.tsx enthaelt die Modulkonstante `HEARTBEAT_TIMEOUT_MS` mit Wert `30_000`
    - poll() filtert das API-Array mit Bedingung `now - new Date(b.status.lastHeartbeat).getTime() <= HEARTBEAT_TIMEOUT_MS` und `if (!b.status?.lastHeartbeat) return false`
    - Die Zeile `setBots(prev => fingerprint(prev) === fingerprint(next) ? prev : next)` ist weiterhin vorhanden
    - npm run build exits 0
  </acceptance_criteria>
  <done>Bots mit Heartbeat aelter als 30s oder ohne Heartbeat erscheinen nach dem naechsten Poll (max. 5s) nicht mehr im bots-State; Build kompiliert fehlerfrei.</done>
</task>

<task type="auto">
  <name>Task 2: Trash-Icon und deleteBot() aus BridgeClient entfernen (BRIDGE-02)</name>
  <files>src/app/bridge/BridgeClient.tsx</files>
  <read_first>
    - src/app/bridge/BridgeClient.tsx (Zeile 5 Import; Zeilen 43-47 deleteBot(); Zeilen 134-149 Tab-Button mit Trash2-Button)
    - .planning/phases/02-bridge-bereinigung/02-PATTERNS.md (Abschnitt "BridgeClient.tsx — Trash-Icon + deleteBot() entfernen", Zeilen 73-118)
    - .planning/phases/02-bridge-bereinigung/02-RESEARCH.md (Pattern 2, Anti-Pattern "deleteBot()-API-Route loeschen")
  </read_first>
  <action>
    Drei zusammenhaengende Entfernungen in BridgeClient.tsx (gemaess D-04):

    1. Import (Zeile 5): Aus `import { Bot, Trash2, TrendingUp, Search, Edit2, Check, X } from 'lucide-react'` ausschliesslich `Trash2` entfernen. Die uebrigen Icons (`Bot, TrendingUp, Search, Edit2, Check, X`) bleiben — sie werden anderswo verwendet.

    2. `deleteBot()`-Funktion (Zeilen 43-47) vollstaendig entfernen — die `async function deleteBot(id: string)` inkl. confirm, fetch DELETE und refresh().

    3. Trash-Button im JSX (Zeilen 144-148): den inneren `<button onClick={e => { e.stopPropagation(); deleteBot(bot.id) }}>` mit `<Trash2 size={12} />` innerhalb des Tab-Buttons (Zeilen 134-149) vollstaendig entfernen. Der aeussere Tab-`<button key={bot.id}>` mit `setSelectedBotId`, dem Status-Punkt `<span>` und `{bot.name}` bleibt vollstaendig erhalten.

    WICHTIG (Anti-Pattern aus RESEARCH): Die API-Route `/api/bots/[id]` DELETE NICHT loeschen — sie wird in Phase 3 noch benoetigt. Es wird ausschliesslich der UI-Aufruf entfernt. `saveBridgeName()` und die uebrige Komponente bleiben unangetastet.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/app/bridge/BridgeClient.tsx Import-Zeile enthaelt NICHT mehr `Trash2`
    - src/app/bridge/BridgeClient.tsx enthaelt NICHT mehr `function deleteBot`
    - src/app/bridge/BridgeClient.tsx enthaelt NICHT mehr `<Trash2`
    - src/app/bridge/BridgeClient.tsx enthaelt weiterhin `setSelectedBotId(bot.id)` im Tab-Button
    - npm run build exits 0
  </acceptance_criteria>
  <done>In der Bridge-Tab-Leiste ist kein Loeschen-Button/Trash-Icon mehr sichtbar; deleteBot() existiert nicht mehr im Code; die DELETE-API-Route ist unveraendert; Build kompiliert fehlerfrei.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bridge/Bot → /api/bridge/status | Untrusted Heartbeat-Daten werden gepollt; lastHeartbeat wird clientseitig ausgewertet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | lastHeartbeat-Timestamp aus API | accept | Rein clientseitige Anzeige-Logik; manipulierter Timestamp blendet hoechstens eine Bridge ein/aus, keine Datenintegritaet oder Privileg betroffen (ASVS L1: kein Access-Control-Impact) |
| T-02-02 | Denial of Service | poll()-Filter bei NaN-Date | mitigate | `if (!b.status?.lastHeartbeat) return false` verhindert NaN-Vergleich (Pitfall 3); kein Crash bei fehlendem Heartbeat |
</threat_model>

<verification>
- npm run build kompiliert ohne Fehler nach beiden Tasks
- Manuell (visuell): Bridge mit altem Heartbeat verschwindet nach max. 5s aus der Liste; kein Trash-Icon in der Bridge-Tab-Leiste sichtbar
- grep: `HEARTBEAT_TIMEOUT_MS` in BotStatusContext.tsx vorhanden; `deleteBot` und `Trash2` in BridgeClient.tsx nicht mehr vorhanden
</verification>

<success_criteria>
- BRIDGE-01: Bridge erscheint/verschwindet automatisch per 30s-Timeout (D-01, D-02, D-03)
- BRIDGE-02: Kein Trash-Icon/Loeschen-Button in der Bridge-UI (D-04)
- `/api/bots/[id]` DELETE-Route bleibt erhalten (fuer Phase 3)
- npm run build exits 0
</success_criteria>

<output>
Create `.planning/phases/02-bridge-bereinigung/02-01-SUMMARY.md` when done
</output>
