---
phase: 02-bridge-bereinigung
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/bridge/log/BridgeLogClient.tsx
  - src/app/bridge/log/page.tsx
  - src/components/layout/Sidebar.tsx
  - src/app/bridge/settings/page.tsx
  - src/app/bridge/settings/BridgeSettingsClient.tsx
autonomous: true
requirements: [BRIDGE-03, BRIDGE-04]
must_haves:
  truths:
    - "Im Bridge-Log ist kein 'Alle Bots'-Button und kein per-Bot-Filter mehr sichtbar (BRIDGE-03, D-05)"
    - "Level-Filter (Info/Warn/Error) und Suchfeld im Bridge-Log bleiben unveraendert erhalten (D-07)"
    - "Das Bridge-Log laedt initial nur Logs von Bridge-Bots (type === 'bridge' oder kein type) (D-08)"
    - "Die Route /bridge/settings existiert nicht mehr — Navigation ergibt 404 (BRIDGE-04, D-09)"
    - "Der Sidebar-Link 'Bridge Settings' ist entfernt (D-10)"
    - "npm run build kompiliert ohne TypeScript-Fehler"
  artifacts:
    - path: "src/app/bridge/log/BridgeLogClient.tsx"
      provides: "Bridge-Log ohne botFilter-State und ohne Bot-Filter-UI; Level-Filter + Suche erhalten"
    - path: "src/app/bridge/log/page.tsx"
      provides: "initialLogs nur aus gefilterter bots-Liste (Bridge-only)"
    - path: "src/components/layout/Sidebar.tsx"
      provides: "BRIDGE_NAV ohne /bridge/settings-Eintrag"
  key_links:
    - from: "src/app/bridge/log/page.tsx"
      to: "BridgeLogClient initialLogs-Prop"
      via: "for (const bot of bots) — gefilterte Liste"
      pattern: "for \\(const bot of bots\\)"
---

<objective>
Der Bridge-Log zeigt nur noch bridge-eigene Eintraege: Der gesamte Bot-Filter-Bereich ("Alle Bots" + per-Bot-Buttons) wird entfernt, und initialLogs laedt nur Logs von Bridge-Bots. Level-Filter und Suchfeld bleiben erhalten. Zusaetzlich wird die Bridge-Settings-Seite vollstaendig geloescht und ihr Sidebar-Link entfernt.

Purpose: Erfuellt BRIDGE-03 (Log-Filter-Bereinigung) und BRIDGE-04 (Settings-Seite entfernen). Beide sind reine Entfernungen von UI-Elementen bzw. Routen ohne Datenfluss-Aenderung.
Output: Bereinigter Bridge-Log + geloeschte Settings-Route + bereinigte Sidebar.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-bridge-bereinigung/02-CONTEXT.md
@.planning/phases/02-bridge-bereinigung/02-RESEARCH.md
@.planning/phases/02-bridge-bereinigung/02-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Dieser Plan erstellt KEINE neuen Symbole. Er entfernt:
- `botFilter` / `setBotFilter`-State in `src/app/bridge/log/BridgeLogClient.tsx`
- Den Bot-Filter-Block und die zugehoerige Trennlinie im JSX
- Den `/bridge/settings`-Eintrag aus `BRIDGE_NAV` in `src/components/layout/Sidebar.tsx`
- Die Dateien `src/app/bridge/settings/page.tsx` und `src/app/bridge/settings/BridgeSettingsClient.tsx`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: botFilter-State und Bot-Filter-UI aus BridgeLogClient entfernen (BRIDGE-03)</name>
  <files>src/app/bridge/log/BridgeLogClient.tsx</files>
  <read_first>
    - src/app/bridge/log/BridgeLogClient.tsx (vollstaendig — botFilter-State Zeile 66; filtered-Logik Zeile 107; Trennlinie Zeile 238; Bot-Filter-Block Zeilen 241-269; gefiltert-Anzeige Zeile 348)
    - .planning/phases/02-bridge-bereinigung/02-PATTERNS.md (Abschnitt "BridgeLogClient.tsx — botFilter entfernen", Zeilen 122-193)
    - .planning/phases/02-bridge-bereinigung/02-RESEARCH.md (Pattern 3, Pitfall 1 zur gefiltert-Anzeige, Open Question zum Log-Loesch-Button)
  </read_first>
  <action>
    Fuenf zusammenhaengende Entfernungen in BridgeLogClient.tsx (gemaess D-05, D-06):

    1. State (Zeile 66): `const [botFilter, setBotFilter] = useState<string>('all')` vollstaendig entfernen.

    2. Filter-Logik in `filtered` (Zeile 107): die Zeile `if (botFilter !== 'all' && e.botId !== botFilter) return false` entfernen. Die uebrigen Filterbedingungen (`levelFilter`, `search.trim()`) bleiben unveraendert.

    3. Trennlinie (Zeile 238): das `<div className="h-4 w-px" style={{ background: 'var(--border)' }} />` zwischen Level- und Bot-Filter entfernen.

    4. Bot-Filter-Block (Zeilen 241-269): den gesamten `<div className="flex items-center gap-1.5 flex-wrap">` mit dem "Alle Bots"-Button (`onClick={() => setBotFilter('all')}`) und der `bots.map(...)`-Button-Liste entfernen.

    5. gefiltert-Anzeige (Zeile 348): den `botFilter`-Term aus der Bedingung entfernen — `{(levelFilter !== 'all' || botFilter !== 'all' || search) && (` wird zu `{(levelFilter !== 'all' || !!search) && (`. KRITISCH (Pitfall 1): wird dieser Term vergessen, schlaegt npm run build mit "Cannot find name 'botFilter'" fehl.

    UNVERAENDERT lassen: die `Props { bots: BotEntry[]; initialLogs: ... }`-Schnittstelle (bots wird weiterhin in fetchAll Zeile 76 und fuer botName in Log-Eintraegen Zeile 377 gebraucht), der Level-Filter (Zeilen 216-236), das Suchfeld (Zeilen 193-213) und der Log-Loesch-Button (`<Trash2 size={13} />` Zeile 335 — gehoert zur Log-Verwaltung, nicht zum Bot-Filter, bleibt per Open-Question-Empfehlung erhalten).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/app/bridge/log/BridgeLogClient.tsx enthaelt NICHT mehr `botFilter`
    - src/app/bridge/log/BridgeLogClient.tsx enthaelt NICHT mehr `setBotFilter`
    - src/app/bridge/log/BridgeLogClient.tsx enthaelt NICHT mehr den Text `Alle Bots`
    - src/app/bridge/log/BridgeLogClient.tsx enthaelt weiterhin `levelFilter` und das Suchfeld
    - src/app/bridge/log/BridgeLogClient.tsx enthaelt weiterhin `bots` in der Props-Schnittstelle
    - npm run build exits 0
  </acceptance_criteria>
  <done>Kein "Alle Bots"-Button und keine per-Bot-Filter-Buttons mehr im Bridge-Log; Level-Filter und Suchfeld funktionieren weiter; Build kompiliert fehlerfrei.</done>
</task>

<task type="auto">
  <name>Task 2: initialLogs auf Bridge-Bots einschraenken (BRIDGE-03)</name>
  <files>src/app/bridge/log/page.tsx</files>
  <read_first>
    - src/app/bridge/log/page.tsx (vollstaendig — Zeilen 20-25: allBots, initialLogs-Loop ueber allBots, bots-Filter)
    - .planning/phases/02-bridge-bereinigung/02-PATTERNS.md (Abschnitt "log/page.tsx — initialLogs auf Bridge-only", Zeilen 196-220)
    - .planning/phases/02-bridge-bereinigung/02-RESEARCH.md (Pattern 4, Pitfall 2)
  </read_first>
  <action>
    Reihenfolge der Definitionen in page.tsx (Zeilen 20-25) tauschen, sodass `initialLogs` nur ueber die gefilterte `bots`-Liste iteriert (gemaess D-08):

    `bots` (`allBots.filter(bot => bot.type === 'bridge' || !bot.type)`, aktuell Zeile 25) VOR den `initialLogs`-Loop ziehen. Den Loop von `for (const bot of allBots)` auf `for (const bot of bots)` umstellen. `allBots = getBots()` bleibt als Quelle fuer die Filterung erhalten.

    Reihenfolge nachher: 1) `const allBots = getBots()`, 2) `const bots = allBots.filter(...)`, 3) `const initialLogs ... = {}`, 4) `for (const bot of bots) { initialLogs[bot.id] = getBridgeLog(bot.id) }`. Das `return`-JSX mit `<BridgeLogClient bots={bots} initialLogs={initialLogs} />` bleibt unveraendert (Pitfall 2: nur so werden initial keine Bot-Logs kurz angezeigt).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/app/bridge/log/page.tsx enthaelt `for (const bot of bots)` (NICHT `for (const bot of allBots)`)
    - Die Definition von `const bots = allBots.filter` steht vor der `initialLogs`-Schleife
    - npm run build exits 0
  </acceptance_criteria>
  <done>initialLogs enthaelt nur Logs von Bridge-Bots; beim ersten Render werden keine Bot-Logs angezeigt; Build kompiliert fehlerfrei.</done>
</task>

<task type="auto">
  <name>Task 3: Bridge-Settings-Seite loeschen + Sidebar-Link entfernen (BRIDGE-04)</name>
  <files>src/app/bridge/settings/page.tsx, src/app/bridge/settings/BridgeSettingsClient.tsx, src/components/layout/Sidebar.tsx</files>
  <read_first>
    - src/components/layout/Sidebar.tsx (Zeilen 5-10 Imports inkl. SlidersHorizontal; BRIDGE_NAV Zeilen 31-35; BOTS_NAV Zeilen 37-43 — SlidersHorizontal in Zeile 40)
    - .planning/phases/02-bridge-bereinigung/02-PATTERNS.md (Abschnitt "Sidebar.tsx — Bridge-Settings-Link entfernen", Zeilen 223-258)
    - .planning/phases/02-bridge-bereinigung/02-RESEARCH.md (Pattern 5, Pitfall 4 zu SlidersHorizontal-Import)
  </read_first>
  <action>
    Drei Aenderungen (gemaess D-09, D-10):

    1. Datei loeschen: `src/app/bridge/settings/page.tsx` — Next.js App Router zeigt danach automatisch 404 fuer /bridge/settings (kein Redirect noetig, D-09).

    2. Datei loeschen: `src/app/bridge/settings/BridgeSettingsClient.tsx`.

    3. In `src/components/layout/Sidebar.tsx` aus dem `BRIDGE_NAV`-Array (Zeilen 31-35) den Eintrag `{ href: '/bridge/settings', label: 'Bridge Settings', icon: SlidersHorizontal },` (Zeile 34) entfernen. BRIDGE_NAV enthaelt danach nur noch `/bridge` und `/bridge/log`.

    KRITISCH (Pitfall 4): Den Import `SlidersHorizontal` (Zeile 7) NICHT entfernen — er wird weiterhin in `BOTS_NAV` Zeile 40 (`{ href: '/bots/settings', ..., icon: SlidersHorizontal }`) verwendet. Wird der Import faelschlich entfernt, schlaegt npm run build fehl.

    Loeschen unter Windows/PowerShell via `Remove-Item` oder dem Filesystem-Tool — beide Dateien vollstaendig entfernen, nicht leeren.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Datei src/app/bridge/settings/page.tsx existiert NICHT mehr
    - Datei src/app/bridge/settings/BridgeSettingsClient.tsx existiert NICHT mehr
    - src/components/layout/Sidebar.tsx enthaelt NICHT mehr `/bridge/settings`
    - src/components/layout/Sidebar.tsx enthaelt weiterhin den Import `SlidersHorizontal`
    - src/components/layout/Sidebar.tsx enthaelt weiterhin `/bots/settings`
    - npm run build exits 0
  </acceptance_criteria>
  <done>Die Route /bridge/settings ergibt 404; der Sidebar-Link 'Bridge Settings' ist entfernt; der SlidersHorizontal-Import bleibt fuer Bot Settings erhalten; Build kompiliert fehlerfrei.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /bridge/settings | Route wird entfernt; verkleinert die Angriffsflaeche (MT5-Zugangsdaten-Bearbeitung nicht mehr ueber UI erreichbar) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Information Disclosure | Geloeschte Bridge-Settings-Seite | mitigate | Loeschen der Settings-Route entfernt eine Oberflaeche, ueber die MT5-Zugangsdaten editierbar waren — verkleinert Angriffsflaeche (ASVS L1) |
| T-02-04 | Elevation of Privilege | Sidebar-Link auf entfernte Route | accept | Toter Link wuerde 404 ergeben; durch Entfernen des BRIDGE_NAV-Eintrags vermieden, kein Privileg-Impact |
</threat_model>

<verification>
- npm run build kompiliert ohne Fehler nach allen drei Tasks
- grep: `botFilter`, `Alle Bots`, `/bridge/settings` nicht mehr in den jeweiligen Dateien vorhanden
- grep: `SlidersHorizontal` weiterhin in Sidebar.tsx vorhanden
- Filesystem: settings/page.tsx und settings/BridgeSettingsClient.tsx existieren nicht mehr
- Manuell (visuell): /bridge/settings ergibt 404; Bridge-Log zeigt nur Level-Filter + Suche
</verification>

<success_criteria>
- BRIDGE-03: Kein "Alle Bots"-Filter im Bridge-Log; nur Bridge-Logs werden geladen (D-05 bis D-08)
- BRIDGE-04: /bridge/settings entfernt (404); Sidebar-Link weg (D-09, D-10)
- SlidersHorizontal-Import bleibt fuer Bot Settings erhalten
- npm run build exits 0
</success_criteria>

<output>
Create `.planning/phases/02-bridge-bereinigung/02-02-SUMMARY.md` when done
</output>
