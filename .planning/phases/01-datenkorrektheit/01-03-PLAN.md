---
phase: 01-datenkorrektheit
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - src/app/bots/BotsClient.tsx
  - src/app/bots/[id]/BotDetailClient.tsx
  - src/components/bridge/WatchdogPanel.tsx
  - src/components/bridge/BridgeDashboardWidget.tsx
autonomous: false
requirements: [SYNC-01, NET-01]
must_haves:
  truths:
    - "Das 'Synced'-Feld ist in keiner Bot-/Bridge-UI mehr sichtbar (SYNC-01)"
    - "Bridge-Netzwerkansicht und Bot-Ansicht zeigen dieselbe Trade-/Positions-Anzahl (NET-01)"
    - "Der tradesSync-Wert (8400+) wird nirgends mehr angezeigt"
  artifacts:
    - path: "src/app/bots/BotsClient.tsx"
      provides: "Bot-Karte ohne Synced-Stat"
    - path: "src/app/bots/[id]/BotDetailClient.tsx"
      provides: "Bot-Detail ohne 'Trades gespeichert'-Stat"
    - path: "src/components/bridge/WatchdogPanel.tsx"
      provides: "Watchdog-Panel ohne Sync-Eintrag"
    - path: "src/components/bridge/BridgeDashboardWidget.tsx"
      provides: "Dashboard-Widget ohne Synced-Anzeige"
  key_links:
    - from: "src/app/bots/BotsClient.tsx"
      to: "BotStatus.tradesSync"
      via: "Entfernung der Synced-Stat-Referenz"
      pattern: "tradesSync"
---

<objective>
SYNC-01 + NET-01 umsetzen. SYNC-01: Der bedeutungslose Sync-Zähler (`tradesSync`, akkumuliert Positions-Zyklen → läuft auf 8400+) wird aus allen vier UI-Stellen entfernt (D-14). Das TypeScript-Feld `tradesSync` in `BotStatus` bleibt für Protokoll-Kompatibilität mit der Python-Bridge erhalten — nur die Anzeige verschwindet. NET-01: Verifizieren, dass Bridge-Netzwerkansicht und Bot-Ansicht nach TRADES-01/TRADES-02 (Plan 01/02) konsistente Zahlen zeigen (D-13). Laut RESEARCH ist NET-01 implizit gelöst, sobald verwaiste offene Trades geschlossen (Plan 02) und sourceId-attributiert (Plan 01) sind — beide Ansichten lesen `openPositions` aus demselben Heartbeat.

Purpose: Irreführende Metriken entfernen und Daten-Konsistenz zwischen Ansichten bestätigen. NET-01 ist der Lackmustest, dass das Trade-Daten-Fundament (Plan 01/02) korrekt greift.
Output: Vier bereinigte UI-Dateien + bestätigte Netzwerk/Bot-Konsistenz.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-datenkorrektheit/01-CONTEXT.md
@.planning/phases/01-datenkorrektheit/01-RESEARCH.md
@.planning/phases/01-datenkorrektheit/01-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Synced-Anzeige aus den vier UI-Stellen entfernen</name>
  <files>src/app/bots/BotsClient.tsx, src/app/bots/[id]/BotDetailClient.tsx, src/components/bridge/WatchdogPanel.tsx, src/components/bridge/BridgeDashboardWidget.tsx</files>
  <read_first>
    - .planning/phases/01-datenkorrektheit/01-PATTERNS.md (vier Abschnitte BotsClient/BotDetailClient/WatchdogPanel/BridgeDashboardWidget — exakte zu entfernende Zeilen + Import-Bereinigung)
    - .planning/phases/01-datenkorrektheit/01-RESEARCH.md (SYNC-01 Verwendungsstellen + Entscheidung: TS-Feld bleibt, nur UI entfernen)
    - src/app/bots/BotsClient.tsx (Stats-Grid Zeilen 164-173 — Synced-Stat Zeile 172)
    - src/app/bots/[id]/BotDetailClient.tsx (Stats-Array Zeilen 148-156 — 'Trades gespeichert' Zeile 156)
    - src/components/bridge/WatchdogPanel.tsx (Import Zeile 4 mit Layers; Stats-Array — Sync-Eintrag mit Layers)
    - src/components/bridge/BridgeDashboardWidget.tsx (totalSync Zeile 34; Synced-JSX-Block)
  </read_first>
  <action>
    Vier Entfernungen, exakt nach 01-PATTERNS.md:

    1. src/app/bots/BotsClient.tsx — im Stats-Grid (grid-cols-2, Zeilen 164-173) die Synced-Zeile entfernen: `<Stat label="Synced" value={status?.tradesSync != null ? \`${status.tradesSync} Trades\` : '-'} />`. Das Grid behält `grid-cols-2` und zeigt dann 3 Stats (Balance, Positionen, Uptime). Keine CSS-Anpassung nötig.

    2. src/app/bots/[id]/BotDetailClient.tsx — im Stats-Array (Zeilen 148-156) den Eintrag `{ label: 'Trades gespeichert', value: String(status?.tradesSync ?? 0) },` entfernen. Array hat dann 7 statt 8 Einträge; `grid-cols-2 sm:grid-cols-4` unverändert.

    3. src/components/bridge/WatchdogPanel.tsx — im Stats-Array den Eintrag `{ Icon: Layers, label: 'Sync', value: \`${status.tradesSync} Trades\`, ... }` entfernen. Anschließend `Layers` aus dem lucide-react-Import (Zeile 4) entfernen, da sonst unbenutzt → Import lautet danach `import { Wifi, WifiOff, AlertTriangle, Bot, Cpu, Clock, TrendingUp } from 'lucide-react'`. Grid behält grid-cols-2 (3 statt 4 Einträge).

    4. src/components/bridge/BridgeDashboardWidget.tsx — die Berechnung `const totalSync = bots.reduce((s, b) => s + (b.status?.tradesSync ?? 0), 0)` (Zeile 34) entfernen UND den zugehörigen JSX-Block, der `{totalSync}` mit Label "Synced" rendert. Der `totalOpen`-Block im selben `flex items-center gap-4`-Container bleibt erhalten; `gap-4`/`shrink-0` unverändert.

    WICHTIG: Das Feld `tradesSync` in src/types/bot.ts (Zeile 49 von BotStatus) NICHT entfernen — bleibt für Python-Protokoll-Kompatibilität (D-14). Keine Datei außerhalb dieser vier ändern.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Keine der vier Dateien rendert `tradesSync` noch als sichtbares Label (Synced / Sync / Trades gespeichert)
    - WatchdogPanel.tsx importiert `Layers` nicht mehr aus lucide-react
    - BridgeDashboardWidget.tsx enthält weder `totalSync` noch den "Synced"-JSX-Block
    - BotsClient.tsx Stats-Grid enthält keinen `Synced`-Stat mehr
    - BotDetailClient.tsx Stats-Array enthält keinen `Trades gespeichert`-Eintrag mehr
    - `tradesSync` bleibt im BotStatus-Typ (src/types/bot.ts) erhalten
    - `npm run build` kompiliert ohne TypeScript-Fehler (keine unbenutzten Imports)
  </acceptance_criteria>
  <done>Sync-Zähler in keiner UI-Stelle mehr sichtbar; BotStatus-Typfeld unverändert; Build grün ohne unbenutzte Imports.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: NET-01 Konsistenz Bridge-Netzwerk vs. Bot-Ansicht verifizieren</name>
  <files>src/app/netzwerk/page.tsx (nur lesen), src/app/bots/BotsClient.tsx (nur lesen)</files>
  <read_first>
    - .planning/phases/01-datenkorrektheit/01-RESEARCH.md (NET-01 Finding — beide Ansichten lesen openPositions aus BotStatus; Mismatch durch verwaiste offene Trades)
    - src/app/netzwerk/page.tsx (was die Netzwerkansicht zählt)
  </read_first>
  <action>
    Dies ist ein reiner Verifikations-Checkpoint — keine Code-Änderung an Produktionscode. NET-01 ist laut RESEARCH implizit durch Plan 01 (sourceId-Attribution) und Plan 02 (Close-Event + Heartbeat-Fallback schließt verwaiste Trades) gelöst, da Netzwerk- und Bot-Ansicht `openPositions` aus demselben Heartbeat lesen (D-13). Der Executor stellt sicher, dass `npm run build` grün ist, und präsentiert dem User die unter <how-to-verify> stehenden manuellen Schritte. Falls der Checkpoint einen verbleibenden Mismatch aufdeckt, wird die Abweichung als Gap dokumentiert (kein Blind-Fix in diesem Plan — Folge-Analyse via /gsd-plan-phase --gaps).
  </action>
  <what-built>
    Mit korrekter Trade-Attribution (Plan 01, sourceId) und Trade-Status-Korrektur (Plan 02, Close-Event + Heartbeat-Fallback) sowie entferntem Sync-Zähler (Task 1) sollten Bridge-Netzwerkansicht und Bot-Ansicht dieselbe Positions-/Trade-Anzahl zeigen. Laut RESEARCH (NET-01 Finding) lesen beide Ansichten `openPositions` aus demselben Heartbeat; der gemeldete 8-vs-1-Mismatch wird durch das Schließen verwaister offener Trades aufgelöst.
  </what-built>
  <how-to-verify>
    1. App starten: `npm run build` muss grün sein, dann `npm run start` (oder Dev-Umgebung mit laufender Bridge).
    2. Sicherstellen, dass die Bridge mindestens einen Heartbeat gesendet hat (ggf. Bridge neu starten).
    3. Netzwerkansicht öffnen: `/netzwerk` — die für Bridge und Bot angezeigte Positions-/Trade-Anzahl notieren.
    4. Bot-Ansicht öffnen: `/bots` — die in der Bot-Karte angezeigte Positionen-Zahl notieren.
    5. Erwartung (NET-01): Für denselben Bot zeigen beide Ansichten dieselbe Zahl — kein 8-vs-1-Mismatch.
    6. Falls weiterhin Mismatch sichtbar: prüfen, ob noch verwaiste offene Trades in `data/bot-trades-{profileId}.json` mit `status: 'open'` existieren, deren Ticket nicht mehr in MT5 ist (dann hat der Heartbeat-Fallback aus Plan 02 noch nicht gegriffen — ggf. weiteren Heartbeat abwarten oder Close-Event prüfen).
    7. Bestätigen, dass das "Synced"-Feld in `/bots`, in der Bot-Detail-Seite, im Watchdog-Panel und im Dashboard-Widget nicht mehr sichtbar ist (SYNC-01).
  </how-to-verify>
  <verify>
    <human-check>Netzwerk- und Bot-Ansicht zeigen für denselben Bot dieselbe Positions-/Trade-Anzahl; kein Synced-Feld mehr sichtbar.</human-check>
  </verify>
  <done>User bestätigt NET-01-Konsistenz und Abwesenheit des Synced-Feldes — oder eine verbleibende Abweichung ist als Gap dokumentiert.</done>
  <resume-signal>Tippe "approved" wenn Netzwerk- und Bot-Ansicht konsistente Zahlen zeigen und kein Synced-Feld mehr sichtbar ist — oder beschreibe die verbleibende Abweichung.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BotStatusContext (Client) → UI-Komponenten | Status-Daten aus Heartbeat werden gerendert; reine Anzeige, keine Mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | Entfernte tradesSync-Anzeige | accept | tradesSync ist eine nicht-sensible interne Metrik; Entfernung reduziert Verwirrung, kein Sicherheitsbezug |
| T-03-02 | Tampering | Unbenutzter Layers-Import nach Entfernung | mitigate | Import wird mitentfernt; `npm run build` schlägt bei unbenutzten Imports im Strict-Mode an |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | Keine neuen Pakete (RESEARCH Package Legitimacy Audit) — reine UI-Entfernungen |
</threat_model>

<verification>
- `npm run build` kompiliert ohne Fehler und ohne unbenutzte Imports
- `tradesSync` wird in keiner der vier UI-Dateien mehr gerendert
- BotStatus-Typ behält `tradesSync`
- Manuelle Checkpoint-Bestätigung: Netzwerk- und Bot-Ansicht zeigen konsistente Zahlen (NET-01)
</verification>

<success_criteria>
- SYNC-01 erfüllt: Sync-Zähler in keiner UI sichtbar (D-14); Typfeld erhalten
- NET-01 erfüllt: Bridge-Netzwerk- und Bot-Ansicht zeigen konsistente Trade-/Positions-Anzahl (D-13), bestätigt durch Checkpoint
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)

| Symbol | Typ | Datei |
|--------|-----|-------|
| Entfernung Synced-Stat | UI-Änderung | src/app/bots/BotsClient.tsx |
| Entfernung 'Trades gespeichert'-Stat | UI-Änderung | src/app/bots/[id]/BotDetailClient.tsx |
| Entfernung Sync-Eintrag + Layers-Import | UI-Änderung | src/components/bridge/WatchdogPanel.tsx |
| Entfernung totalSync + Synced-Block | UI-Änderung | src/components/bridge/BridgeDashboardWidget.tsx |
| NET-01-Konsistenz (verifiziert) | Verhaltens-Bestätigung | Netzwerk-/Bot-Ansicht |
</artifacts_produced>

<output>
Create `.planning/phases/01-datenkorrektheit/01-03-SUMMARY.md` when done
</output>
