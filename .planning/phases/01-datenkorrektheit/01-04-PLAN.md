---
phase: 01-datenkorrektheit
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - bridge/trade_executor.py
autonomous: true
requirements: [TRADES-03]
must_haves:
  truths:
    - "Trade-Executor-Eröffnungsorders schreiben '/bridge/tradeexecuter' in den MT5-Kommentar"
    - "Close-Order-Kommentar bleibt unverändert"
  artifacts:
    - path: "bridge/trade_executor.py"
      provides: "MT5-Kommentar '/bridge/tradeexecuter' bei Eröffnungsorders"
      contains: "/bridge/tradeexecuter"
  key_links:
    - from: "bridge/trade_executor.py"
      to: "MT5 order_send request"
      via: "comment-Feld"
      pattern: "bridge/tradeexecuter"
---

<objective>
TRADES-03 umsetzen: Trade-Executor-Trades enthalten `/bridge/tradeexecuter` im MetaTrader-Kommentar (D-08). Dies geschieht ausschließlich in der Python-Bridge — AlphaTrack liest den Kommentar nur. Eine einzeilige Änderung im `request`-Dict der Eröffnungsorder.

Purpose: Der MT5-Kommentar ist die Quell-Markierung auf MT5-Seite, die Trade-Executor-Trades eindeutig identifizierbar macht. Komplementär zur sourceId-Attribution auf AlphaTrack-Seite (Plan 01).
Output: Geänderter comment-Wert in bridge/trade_executor.py.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-datenkorrektheit/01-CONTEXT.md
@.planning/phases/01-datenkorrektheit/01-RESEARCH.md
@.planning/phases/01-datenkorrektheit/01-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: MT5-Kommentar auf '/bridge/tradeexecuter' setzen</name>
  <files>bridge/trade_executor.py</files>
  <read_first>
    - bridge/trade_executor.py (request-Dict Zeilen 80-91 — aktueller Wert `"comment": "AlphaTrack Executor"` Zeile 88; Close-Order-Kommentar Zeile ~183 NICHT ändern)
    - .planning/phases/01-datenkorrektheit/01-PATTERNS.md (Abschnitt "bridge/trade_executor.py" — exakter 1-Zeilen-Fix + Hinweis Close-Order bleibt)
  </read_first>
  <action>
    In bridge/trade_executor.py im `request`-Dict der Eröffnungsorder (Zeile 88) den Wert des `comment`-Feldes von `"AlphaTrack Executor"` auf `"/bridge/tradeexecuter"` ändern. Die Zeile lautet danach exakt: `"comment": "/bridge/tradeexecuter",`. Der String hat 21 Zeichen und liegt unter dem MT5-Kommentarlimit (~31 Zeichen, RESEARCH A1). KEINE weitere Zeile ändern — insbesondere der Close-Order-Kommentar (`"comment": "AlphaTrack Close"`, ca. Zeile 183) bleibt unverändert, da TRADES-03 nur Eröffnungsorders betrifft.
  </action>
  <verify>
    <automated>powershell -Command "if ((Select-String -Path 'bridge/trade_executor.py' -Pattern '/bridge/tradeexecuter' -SimpleMatch | Measure-Object).Count -ge 1) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - bridge/trade_executor.py enthält die Zeichenkette `/bridge/tradeexecuter` im comment-Feld der Eröffnungs-`request`
    - Der String `AlphaTrack Executor` ist im Eröffnungs-request nicht mehr vorhanden
    - Der Close-Order-Kommentar (`AlphaTrack Close`) ist unverändert vorhanden
  </acceptance_criteria>
  <done>Eröffnungsorders tragen den Kommentar '/bridge/tradeexecuter'; Close-Order-Kommentar unverändert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bridge (Python) → MT5 order_send | Bridge setzt comment-Feld; MT5 ist vertrauenswürdige lokale Komponente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | comment-String-Länge | mitigate | `/bridge/tradeexecuter` = 21 Zeichen < MT5-Limit ~31 (RESEARCH A1); kein Truncation-Risiko |
| T-04-02 | Information Disclosure | comment im MT5-Terminal sichtbar | accept | Enthält keine Secrets, nur Quell-Marker; bewusst gewünscht (TRADES-03) |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | Keine neuen Pakete (RESEARCH Package Legitimacy Audit) — reine Wertänderung |
</threat_model>

<verification>
- bridge/trade_executor.py enthält `/bridge/tradeexecuter` im Eröffnungs-comment
- `AlphaTrack Executor` ist aus dem Eröffnungs-request entfernt
- Close-Order-Kommentar unverändert
</verification>

<success_criteria>
- TRADES-03 erfüllt: Trade-Executor-Eröffnungsorders enthalten `/bridge/tradeexecuter` im MT5-Kommentar (D-08)
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 04)

| Symbol | Typ | Datei |
|--------|-----|-------|
| comment = `/bridge/tradeexecuter` | MT5-order_send-Kommentarwert | bridge/trade_executor.py |
</artifacts_produced>

<output>
Create `.planning/phases/01-datenkorrektheit/01-04-SUMMARY.md` when done
</output>
