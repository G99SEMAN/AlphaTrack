---
phase: 03-bot-verbesserungen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/bot.ts
  - src/app/api/bots/[id]/stats/route.ts
  - src/app/api/bridge/command/route.ts
autonomous: true
requirements: [BOTS-01, BOTS-03, BOTS-04, BOTS-08]
must_haves:
  truths:
    - "GET /api/bots/:id/stats liefert openCount, tradeCount, realizedPnl und currency für einen bekannten Bot"
    - "realizedPnl ist null wenn der Bot keine geschlossenen Trades hat (nicht 0)"
    - "POST /api/bridge/command mit command='set_parameters' und gültigem parameters-Objekt wird akzeptiert (kein 400)"
    - "POST /api/bridge/command mit command='set_parameters' ohne parameters-Objekt wird mit 400 abgelehnt"
    - "BotStatus akzeptiert ein optionales parameters-Feld vom Heartbeat ohne Typfehler"
  artifacts:
    - path: "src/types/bot.ts"
      provides: "BotCommandType erweitert um 'set_parameters'; BotStatus.parameters?; SetParametersPayload; BotStats"
      contains: "set_parameters"
    - path: "src/app/api/bots/[id]/stats/route.ts"
      provides: "GET-Endpunkt der Trade-Metriken pro Bot aggregiert"
      exports: ["GET"]
    - path: "src/app/api/bridge/command/route.ts"
      provides: "Command-Route die set_parameters validiert und weiterleitet"
      contains: "set_parameters"
  key_links:
    - from: "src/app/api/bots/[id]/stats/route.ts"
      to: "getProfileTrades"
      via: "Trade-Filterung nach sourceId === botId"
      pattern: "getProfileTrades\\(bot\\.profileId\\)"
    - from: "src/app/api/bridge/command/route.ts"
      to: "VALID_COMMANDS"
      via: "set_parameters in der Whitelist"
      pattern: "set_parameters"
---

<objective>
Daten-Layer und Backend für die Bot-Metriken und Parameter-Updates schaffen. Dieser Plan definiert die Typ-Verträge (`BotCommandType`, `BotStatus.parameters`, `SetParametersPayload`, `BotStats`), erstellt den neuen `GET /api/bots/:id/stats`-Endpunkt der Trade-Daten server-seitig aggregiert (D-01, D-02, D-03, D-04, D-06), und erweitert die Command-Route um den `set_parameters`-Command (D-09).

Purpose: Alle UI-Pläne (02, 03) konsumieren die hier definierten Typen und Endpunkte. Ohne dieses Fundament gibt es weder korrekte Metriken (BOTS-01/03/04) noch Parameter-Updates (BOTS-08).
Output: Erweiterte `src/types/bot.ts`, neuer Stats-Endpunkt, erweiterte Command-Route.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-bot-verbesserungen/03-CONTEXT.md
@.planning/phases/03-bot-verbesserungen/03-RESEARCH.md
@.planning/phases/03-bot-verbesserungen/03-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Symbole, die in dieser Phase NEU erstellt werden (für source-grounding der Review-Konvergenz — diese gelten nicht als Drift):

- `set_parameters` — neuer Wert in der Union `BotCommandType` (src/types/bot.ts)
- `SetParametersPayload` — neues Interface (src/types/bot.ts)
- `BotStats` — neues Interface (src/types/bot.ts)
- `BotStatus.parameters` — neues optionales Feld `parameters?: Record<string, string | number | boolean>` (src/types/bot.ts)
- `GET /api/bots/[id]/stats` — neue Route-Datei `src/app/api/bots/[id]/stats/route.ts`
- `formatPnl` — neue Helfer-Funktion in src/app/bots/BotsClient.tsx (Plan 02)
- `sendParameters`, `renderParameterEditor`, `drafts`, `sending` — neue Symbole in src/app/bots/settings/BotsSettingsClient.tsx (Plan 03)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Typ-Verträge in bot.ts erweitern</name>
  <files>src/types/bot.ts</files>
  <read_first>
    - src/types/bot.ts (aktueller Stand: BotCommandType Z.5, BotStatus Z.42-53, ClosePositionPayload Z.7-9, TradeOrderPayload Z.11-19)
    - src/app/api/bridge/heartbeat/route.ts (D-08: prüfen dass Body-Typisierung das neue parameters-Feld ohne explizite Änderung durchreicht — saveBotStatus spreaded den gesamten Status inkl. parameters)
    - .planning/phases/03-bot-verbesserungen/03-PATTERNS.md (Abschnitt "src/types/bot.ts" — exakte Erweiterungs-Snippets)
  </read_first>
  <action>
    Erweitere `src/types/bot.ts` um vier Änderungen (per D-07, D-08, D-09, D-02):
    1. Union-Typ `BotCommandType` (Z.5): den String-Literal `'set_parameters'` an die bestehende Union anhängen — bestehende Werte unverändert lassen.
    2. Interface `BotStatus` (Z.42-53): nach dem bestehenden Feld `currency?: number` ein neues optionales Feld hinzufügen: `parameters?: Record<string, string | number | boolean>`. Optional (`?`) wegen Rückwärtskompatibilität — alte Bots senden kein parameters-Feld.
    3. Neues Interface `SetParametersPayload` mit einem Feld `parameters: Record<string, string | number | boolean>` — analog zu `ClosePositionPayload`/`TradeOrderPayload` platzieren (nach Z.19).
    4. Neues Interface `BotStats` mit den Feldern `openCount: number`, `tradeCount: number`, `realizedPnl: number | null` (null = keine geschlossenen Trades, D-06) und `currency: string`.
    Keine bestehenden Felder löschen oder umbenennen.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` läuft ohne Fehler durch
    - `BotCommandType` enthält den Literal `'set_parameters'` (grep `set_parameters` in src/types/bot.ts findet Treffer)
    - `BotStatus` enthält das Feld `parameters?: Record<string, string | number | boolean>`
    - Interface `SetParametersPayload` existiert mit Feld `parameters`
    - Interface `BotStats` existiert mit Feldern `openCount`, `tradeCount`, `realizedPnl: number | null`, `currency`
    - Kein bestehendes Feld in BotStatus/BotCommandType wurde entfernt
    - D-08 verifiziert: `npx tsc --noEmit` zeigt keinen Fehler in `heartbeat/route.ts` — das `parameters`-Feld wird via `saveBotStatus(id, { ...status, lastHeartbeat })` automatisch persistiert ohne Typfehler (Status-Spread deckt optionale Felder ab)
  </acceptance_criteria>
  <done>bot.ts kompiliert; alle vier neuen Symbole vorhanden; bestehende Verträge intakt.</done>
</task>

<task type="auto">
  <name>Task 2: Stats-Endpunkt /api/bots/[id]/stats erstellen</name>
  <files>src/app/api/bots/[id]/stats/route.ts</files>
  <read_first>
    - src/app/api/bots/[id]/log/route.ts (Analog-Muster: GET-Signatur mit `params: Promise<{ id: string }>`, `await params`, `getBotById`-Lookup, NextResponse.json)
    - src/lib/profiles.ts (Z.101: `getProfileTrades(profileId): Trade[]` — liest data/trades-{profileId}.json; `getProfiles()`)
    - src/types/trade.ts (Trade-Felder: `sourceId?: string`, `status: 'open'|'closed'|'cancelled'`, `pnl?: number`)
    - src/lib/auth.ts (isSameOriginRequest)
    - .planning/phases/03-bot-verbesserungen/03-PATTERNS.md (Abschnitt "stats/route.ts" — vollständiges Core-Pattern + Pitfall 1)
  </read_first>
  <action>
    Erstelle `src/app/api/bots/[id]/stats/route.ts` mit einem `GET`-Handler nach dem Muster von `log/route.ts` (per D-02, D-03, D-04, D-06):
    - Signatur: `GET(req: NextRequest, { params }: { params: Promise<{ id: string }> })`, dann `const { id } = await params`.
    - Same-Origin-Gate: bei `!isSameOriginRequest(req)` mit Status 403 und `{ error: 'Forbidden' }` antworten (Frontend-only Endpunkt).
    - `const bot = getBotById(id)`; bei `!bot` mit Status 404 und `{ error: 'Bot nicht gefunden' }` antworten.
    - Trades laden mit `getProfileTrades(bot.profileId)` aus `@/lib/profiles` — KRITISCH: NICHT `getTrades()` aus `@/lib/data` (das liest die hardcodierte data/trades.json, Pitfall 1).
    - Filtern: `botTrades = trades.filter(t => t.sourceId === id)`.
    - `openCount` = Anzahl `botTrades` mit `status === 'open'`.
    - `tradeCount` = `botTrades.length`.
    - `realizedPnl`: aus den `botTrades` mit `status === 'closed' && t.pnl !== undefined`. Wenn diese Menge leer ist → `realizedPnl = null` (D-06). Sonst Summe der `pnl`-Werte via reduce.
    - `currency`: aus `getProfiles().find(p => p.id === bot.profileId)?.currency ?? 'EUR'`.
    - Response: `NextResponse.json({ openCount, tradeCount, realizedPnl, currency })` — Form muss dem Interface `BotStats` aus src/types/bot.ts entsprechen.
    Optional `BotStats` aus `@/types/bot` importieren und das Response-Objekt typisieren.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - Datei `src/app/api/bots/[id]/stats/route.ts` existiert und exportiert eine `GET`-Funktion
    - `npx tsc --noEmit` läuft ohne Fehler durch
    - grep `getProfileTrades(bot.profileId)` in der Datei findet Treffer (korrekte profil-spezifische Trade-Quelle, Pitfall 1)
    - grep `getTrades` in der Datei findet KEINEN Treffer (die hardcodierte Variante wird nicht verwendet)
    - Die Response enthält die Schlüssel openCount, tradeCount, realizedPnl, currency
    - realizedPnl wird auf null gesetzt wenn keine geschlossenen Trades vorhanden sind (sichtbar im Code als Verzweigung leere-Menge → null)
  </acceptance_criteria>
  <done>Stats-Endpunkt liest profil-spezifische Trades, filtert nach sourceId, gibt korrekt geformtes BotStats-JSON zurück; realizedPnl=null bei keinen closed-Trades.</done>
</task>

<task type="auto">
  <name>Task 3: Command-Route um set_parameters erweitern</name>
  <files>src/app/api/bridge/command/route.ts</files>
  <read_first>
    - src/app/api/bridge/command/route.ts (aktueller Stand: VALID_COMMANDS Z.6, body-Typ Z.13, Validierungsblöcke execute_trade Z.25-33 und close_position Z.35-40, Flask-Weiterleitung Z.58-60)
    - src/types/bot.ts (nach Task 1: SetParametersPayload, BotCommandType mit set_parameters)
    - .planning/phases/03-bot-verbesserungen/03-PATTERNS.md (Abschnitt "command/route.ts" + Pitfall 4: addBotCommand bleibt unverändert)
  </read_first>
  <action>
    Erweitere `src/app/api/bridge/command/route.ts` (per D-09):
    1. Import-Zeile (Z.3): `SetParametersPayload` aus `@/types/bot` zur bestehenden Import-Liste (BotCommandType, TradeOrderPayload, ClosePositionPayload) hinzufügen.
    2. `VALID_COMMANDS` (Z.6): den Wert `'set_parameters'` an das Array anhängen.
    3. body-Typ (Z.13): den `payload?`-Union-Typ um `| SetParametersPayload` erweitern.
    4. Validierungsblock nach dem close_position-Block (nach Z.40) hinzufügen: bei `command === 'set_parameters'` prüfen, dass `payload.parameters` ein Objekt ist (`typeof p.parameters === 'object' && !Array.isArray(p.parameters)`). Bei Verstoß: `NextResponse.json({ error: 'set_parameters requires parameters object' }, { status: 400 })`.
    `addBotCommand(bridgeId, command)` NICHT ändern (Pitfall 4) — der Payload wird bereits über `flaskBody.payload = payload` (Z.60) an Flask weitergeleitet. Die bestehende Flask-Weiterleitungslogik (Z.58-84) bleibt unverändert; `set_parameters` fällt in den Standard-Pfad (kein execute_trade/close_position Sonderfall nötig).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` läuft ohne Fehler durch
    - grep `set_parameters` in command/route.ts findet mindestens 2 Treffer (VALID_COMMANDS + Validierungsblock)
    - VALID_COMMANDS enthält 'set_parameters'
    - Es existiert ein Validierungsblock `if (command === 'set_parameters')` der bei fehlendem/ungültigem parameters-Objekt Status 400 zurückgibt
    - `SetParametersPayload` ist importiert
    - `addBotCommand` wurde nicht verändert (Signatur weiterhin `addBotCommand(bridgeId, command)`)
  </acceptance_criteria>
  <done>Command-Route akzeptiert set_parameters als validen Command, validiert das parameters-Objekt und leitet Payload via bestehende Flask-Logik weiter.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/bots/:id/stats | Frontend liest Bot-Metriken; nur Same-Origin erlaubt |
| Browser → /api/bridge/command | Frontend sendet set_parameters; nur Same-Origin erlaubt |
| Bot (Flask) ← AlphaTrack /command | AlphaTrack leitet Parameter-Payload an Flask-Bot weiter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Spoofing | /api/bots/:id/stats | mitigate | `isSameOriginRequest(req)` Gate (403 bei fremdem Origin); Bot-Lookup via `getBotById(id)` → unbekannte IDs ergeben 404 |
| T-03-02 | Tampering | set_parameters Payload | mitigate | Typ-Prüfung `typeof parameters === 'object' && !Array.isArray` in command/route.ts; Flask-Bot ist für inhaltliche Validierung der Parameterwerte zuständig (V5 ASVS L1, dokumentierte Grenze) |
| T-03-03 | Spoofing | set_parameters an fremden Bot | mitigate | `getBotById(bridgeId)` → Command nur an bekannte Bot-IDs (404 sonst); bestehender `isSameOriginRequest`-Gate bleibt |
| T-03-04 | Information Disclosure | Stats-Endpunkt liest Trade-Daten | accept | Kein neues Risiko — `getProfileTrades()` ist bestehende, geprüfte Leseschicht; lokale Single-User-Installation ohne PII-Exposition |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | Entfällt — Phase 3 installiert keine Pakete (RESEARCH.md: Package Legitimacy Audit „Entfällt") |
</threat_model>

<verification>
- `npx tsc --noEmit -p tsconfig.json` kompiliert ohne Fehler
- `npm run build` schließt erfolgreich ab
- Manuell: `GET /api/bots/{bekannte-bot-id}/stats` liefert JSON mit openCount/tradeCount/realizedPnl/currency
- Manuell: `POST /api/bridge/command` mit `{ bridgeId, command: 'set_parameters', payload: { parameters: { lots: 0.1 } } }` ergibt kein 400
- Manuell: gleicher POST ohne `payload.parameters` ergibt 400
</verification>

<success_criteria>
- bot.ts enthält set_parameters, BotStatus.parameters, SetParametersPayload, BotStats
- Stats-Endpunkt aggregiert profil-spezifische Trade-Daten korrekt (sourceId-Filter, realizedPnl=null bei keinen closed-Trades)
- Command-Route validiert und akzeptiert set_parameters
- Build grün
</success_criteria>

<output>
Create `.planning/phases/03-bot-verbesserungen/03-01-SUMMARY.md` when done
</output>
