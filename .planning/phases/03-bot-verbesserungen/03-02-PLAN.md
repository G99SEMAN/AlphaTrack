---
phase: 03-bot-verbesserungen
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/app/bots/BotsClient.tsx
autonomous: false
requirements: [BOTS-01, BOTS-02, BOTS-03, BOTS-04, BOTS-05]
must_haves:
  truths:
    - "Bot-Karte zeigt die tatsächliche Anzahl offener Positionen aus den Trade-Daten (nicht status.openPositions)"
    - "Bot-Karte zeigt realisierten P&L des Bots statt Balance, farbig (grün positiv / rot negativ)"
    - "Bot ohne geschlossene Trades zeigt im P&L-Feld '-' statt '0.00'"
    - "Bot-Karte zeigt die Gesamt-Trade-Anzahl des Bots"
    - "Kein 'Synced'-Feld ist in der Bot-Karte vorhanden (Bestätigung, bereits erfüllt)"
    - "Ein getrennter Bot verschwindet aus der Liste (Bestätigung, bereits erfüllt)"
  artifacts:
    - path: "src/app/bots/BotsClient.tsx"
      provides: "Bot-Karte mit P&L/Positionen/Trades/Uptime aus /api/bots/:id/stats"
      contains: "/api/bots/"
  key_links:
    - from: "src/app/bots/BotsClient.tsx"
      to: "/api/bots/:id/stats"
      via: "fetch im stats-Polling-useEffect (8s Interval)"
      pattern: "fetch\\(`/api/bots/\\$\\{.*\\}/stats`\\)"
    - from: "src/app/bots/BotsClient.tsx"
      to: "BotStats"
      via: "Typ-Import aus @/types/bot"
      pattern: "BotStats"
---

<objective>
Die Bot-Karte in `BotsClient.tsx` von Heartbeat-Metriken auf Trade-Daten-Metriken umbauen. Statt `status.balance` und `status.openPositions` (Root Cause BOTS-01: meldet 0) zeigt die Karte nun P&L, offene Positionen, Trades und Uptime aus dem `/api/bots/:id/stats`-Endpunkt (D-01). P&L wird farbig und vorzeichenbehaftet dargestellt (D-05, D-06).

Purpose: Erfüllt BOTS-01 (korrekte Positionen), BOTS-03 (P&L statt Balance), BOTS-04 (Trade-Anzahl). Bestätigt BOTS-02 (Synced bereits weg) und BOTS-05 (Auto-Disappear bereits aktiv) per UAT.
Output: Umgebautes `BotsClient.tsx` mit Stats-Polling und 4-Kachel-Grid.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-bot-verbesserungen/03-CONTEXT.md
@.planning/phases/03-bot-verbesserungen/03-RESEARCH.md
@.planning/phases/03-bot-verbesserungen/03-PATTERNS.md
@.planning/phases/03-bot-verbesserungen/03-UI-SPEC.md
@.planning/phases/03-bot-verbesserungen/03-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Neue Symbole in diesem Plan (kein Drift):
- `stats` State (`Record<string, BotStats>`) in BotsClient.tsx
- `formatPnl(realizedPnl, currency)` Helfer in BotsClient.tsx
- `valueColor?`-Prop an der bestehenden `Stat`-Komponente
- Konsumiert: `BotStats`, `GET /api/bots/[id]/stats` (aus Plan 01)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Stats-Polling + Stat-Komponente erweitern</name>
  <files>src/app/bots/BotsClient.tsx</files>
  <read_first>
    - src/app/bots/BotsClient.tsx (aktueller Stand: Imports Z.1-9, filterBots Z.68-69, refresh-useEffect Z.72-85, Stats-Grid Z.164-172, Stat-Komponente Z.201-208)
    - src/types/bot.ts (BotStats-Interface aus Plan 01)
    - src/lib/currency.ts (currencySymbol — bereits in BotsClient importiert, Z.9)
    - .planning/phases/03-bot-verbesserungen/03-PATTERNS.md (Abschnitt "BotsClient.tsx" — Stats-Polling-Snippet, formatPnl, Pitfall 3)
    - .planning/phases/03-bot-verbesserungen/03-UI-SPEC.md (Abschnitt "States & Interactions: Bot-Karte Stats-Loading" + P&L-Farbregel)
  </read_first>
  <action>
    Erweitere `BotsClient.tsx` (per D-01, D-05, D-06):
    1. Import: `BotStats` aus `@/types/bot` zur bestehenden Type-Import-Zeile (Z.6) hinzufügen (`useEffect` ist bereits importiert).
    2. Stat-Komponente (Z.201-208): optionalen Prop `valueColor?: string` hinzufügen; im `<p>` für den Wert `style={{ color: valueColor ?? 'var(--text-1)' }}` setzen statt fest `var(--text-1)`.
    3. Stats-State im Component-Body: `const [stats, setStats] = useState<Record<string, BotStats>>({})`.
    4. Stats-Polling-useEffect (Abhängigkeit `[bots]`): mit `Promise.allSettled` für jeden Bot `fetch(\`/api/bots/${bot.id}/stats\`)`; nur `res.ok`-Responses übernehmen; Ergebnis als `Record<botId, BotStats>` in setStats schreiben. Initial sofort fetchen, dann `setInterval(fetchStats, 8000)`; im Cleanup `clearInterval`. Pitfall 3 beachten: State als Record führen, beim Rendern nur `stats[bot.id]` lesen — verschwundene Bots hinterlassen veraltete Einträge, die nie gerendert werden.
    5. P&L-Formatierungs-Helfer `formatPnl(realizedPnl: number | null, currency: string): { value: string; color: string }` (Modul-Funktion, analog `formatUptime`): bei `null` → `{ value: '-', color: 'var(--text-3)' }` (D-06); bei `> 0` → `+{toFixed(2)} {currencySymbol(currency)}` in `var(--green)`; bei `< 0` → `{toFixed(2)} {currencySymbol(currency)}` in `var(--red)`; bei `=== 0` → `+0.00 {currencySymbol(currency)}` in `var(--text-1)` (D-05, UI-SPEC P&L-Farbregel). Hinweis: UI-SPEC schreibt `var(--red)` vor (nicht das frühere inline `#ef4444`).
    Keine Änderung an filterBots (Z.68-69) — BOTS-05 ist dort bereits erfüllt.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` läuft ohne Fehler durch
    - grep `fetch(`/api/bots/` in BotsClient.tsx findet einen Treffer im stats-Polling
    - `stats`-State vom Typ `Record<string, BotStats>` existiert
    - `Stat` akzeptiert `valueColor?: string` und verwendet es für die Wert-Farbe
    - `formatPnl` gibt bei `null` `{ value: '-', color: 'var(--text-3)' }` zurück
    - `formatPnl` verwendet `var(--green)` für positiv und `var(--red)` für negativ (gemäß UI-SPEC)
    - Polling-Interval beträgt 8000ms mit clearInterval im Cleanup
  </acceptance_criteria>
  <done>Stats werden alle 8s pro Bot gefetcht; Stat-Komponente unterstützt Farbe; formatPnl erfüllt D-05/D-06.</done>
</task>

<task type="auto">
  <name>Task 2: Stats-Grid auf P&L/Positionen/Trades/Uptime umbauen</name>
  <files>src/app/bots/BotsClient.tsx</files>
  <read_first>
    - src/app/bots/BotsClient.tsx (Stats-Grid Z.163-172 — aktuell Balance/Positionen/Uptime; Render-Loop Z.136-193 mit `{ bot, status }` und `profile`)
    - .planning/phases/03-bot-verbesserungen/03-UI-SPEC.md (Component Inventory #1 — Reihenfolge P&L/Positionen/Trades/Uptime; Copywriting: Labels "P&L","Positionen","Trades","Uptime")
    - .planning/phases/03-bot-verbesserungen/03-PATTERNS.md (Abschnitt "Stats-Grid umbauen")
  </read_first>
  <action>
    Baue das Stats-Grid im Karten-Render (Z.164-172) um (per BOTS-01, BOTS-03, BOTS-04, D-05, D-06):
    - Im Render-Body je Bot: `const botStats = stats[bot.id]` und `const pnl = formatPnl(botStats?.realizedPnl ?? null, botStats?.currency ?? profile?.currency ?? 'EUR')`.
    - Grid bleibt `grid grid-cols-2 gap-2`. Vier `<Stat>`-Kacheln in dieser Reihenfolge (UI-SPEC):
      1. `<Stat label="P&L" value={pnl.value} valueColor={pnl.color} />`
      2. `<Stat label="Positionen" value={botStats?.openCount?.toString() ?? '-'} />` — aus Trade-Daten, NICHT mehr `status.openPositions` (Root Cause BOTS-01).
      3. `<Stat label="Trades" value={botStats?.tradeCount?.toString() ?? '-'} />` (BOTS-04, neu).
      4. `<Stat label="Uptime" value={status?.uptime ? formatUptime(status.uptime) : '-'} />` (bestehend, unverändert).
    - Die alte `Balance`-Kachel (Z.165-169) vollständig entfernen (BOTS-03).
    - Bei initialem Zustand ohne geladene Stats zeigen alle Werte `-` (UI-SPEC States) — durch die `?? '-'`-Fallbacks bereits gegeben.
    `currencySymbol`-Import bleibt; `status?.balance`-Referenz entfällt damit aus dem Grid.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` läuft ohne Fehler durch
    - grep `label="P&L"` und `label="Trades"` in BotsClient.tsx finden je einen Treffer
    - grep `label="Balance"` in BotsClient.tsx findet KEINEN Treffer (Balance-Kachel entfernt)
    - Positionen-Kachel liest `botStats?.openCount` (nicht `status.openPositions`)
    - P&L-Kachel erhält `valueColor={pnl.color}`
    - Vier Stat-Kacheln in der Reihenfolge P&L, Positionen, Trades, Uptime
  </acceptance_criteria>
  <done>Bot-Karte zeigt P&L (farbig), Positionen (aus Trades), Trades, Uptime; keine Balance/Synced-Kachel.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UAT — Bot-Karte Metriken visuell verifizieren</name>
  <action>Manueller Verifikationsschritt — keine Code-Änderung. Der Ausführende baut und startet die App und prüft die unter how-to-verify gelisteten Punkte (BOTS-01/02/03/04/05) im Browser, dann pausiert er bis zum resume-signal.</action>
  <what-built>
    BotsClient.tsx zeigt jetzt 4 Stat-Kacheln: P&L (farbig, aus realisierten Trades), Positionen (offene Trades aus Trade-Daten), Trades (Gesamtanzahl), Uptime. Balance- und Synced-Felder sind weg. Stats werden alle 8s vom neuen /api/bots/:id/stats-Endpunkt geladen. Auto-Disappear bei Disconnect (BOTS-05) ist über den bestehenden offline-Filter aktiv.
  </what-built>
  <how-to-verify>
    1. `npm run build` läuft ohne Fehler. Dann `npm run dev` und Browser auf http://localhost:3000/bots öffnen (bei laufendem Bot mit Trades).
    2. BOTS-01: Bot-Karte zeigt unter "Positionen" die tatsächliche Anzahl offener Trades des Bots (nicht 0, sofern offene Trades existieren).
    3. BOTS-03: Bot-Karte zeigt "P&L" statt "Balance" — bei profitablem Bot grün mit `+`, bei Verlust rot mit `-`. Bot ohne geschlossene Trades zeigt `-`.
    4. BOTS-04: Bot-Karte zeigt "Trades" mit der Gesamtanzahl der Bot-Trades.
    5. BOTS-02: Es gibt KEIN "Synced"-Feld in der Karte.
    6. BOTS-05: Stoppe den Bot / lass den Heartbeat aussetzen — nach spätestens ~30s verschwindet die Karte aus der Liste, ohne Klick auf einen Entfernen-Button.
  </how-to-verify>
  <resume-signal>Schreibe "approved" oder beschreibe Abweichungen (z.B. falsche Positionen, P&L-Farbe falsch).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/bots/:id/stats | BotsClient fetcht Metriken; Same-Origin durch Endpunkt erzwungen (Plan 01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Denial of Service | Stats-Polling alle 8s × N Bots | accept | Lokale Single-User-Installation, kleine Bot-Anzahl; `Promise.allSettled` verhindert Blockade bei Einzelfehler; gleicher Interval wie bestehender Status-Refresh |
| T-03-06 | Information Disclosure | P&L-Werte im Frontend | accept | Kein neues Risiko — Same-Origin-Gate am Endpunkt; lokale Installation ohne fremde Clients |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | Entfällt — keine neuen Pakete (RESEARCH.md Audit „Entfällt") |
</threat_model>

<verification>
- `npx tsc --noEmit -p tsconfig.json` und `npm run build` erfolgreich
- Visuelle UAT laut Checkpoint: BOTS-01/02/03/04/05 bestätigt
</verification>

<success_criteria>
- Bot-Karte zeigt P&L (farbig), Positionen (aus Trades), Trades, Uptime
- Keine Balance- oder Synced-Kachel
- Stats per 8s-Polling vom Stats-Endpunkt
- Auto-Disappear bestätigt
</success_criteria>

<output>
Create `.planning/phases/03-bot-verbesserungen/03-02-SUMMARY.md` when done
</output>
