---
context: phase
phase: 03-bot-verbesserungen
task: 3
total_tasks: 9
status: in_progress
last_updated: 2026-06-11T16:40:12.593Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints identified — proceed directly to current state._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Trade attribution gap | Stats-Endpunkt filtert nach `sourceId === botId`, aber Bridge-Sync setzt `sourceId = "bridge/tradeexecuter"` als Fallback wenn kein `bot_id` im Trade | advisory | Bereits gefixt (commit 22610ed) — Bridge/Gateway nun mit Ticket-Registry. UAT muss nach Bridge+Bot-Neustart wiederholt werden. |

<current_state>
Phase 03, Wave 2 läuft. Plan 01 abgeschlossen (3 Commits + SUMMARY.md). Pläne 02 und 03 haben ihre Code-Tasks fertig (je 2 Commits), sind aber beim UAT-Checkpoint (Task 3) gestoppt — warten auf manuelle visuelle Verifikation im Browser.

Ein separater Bug in der Bridge (`bridge/gateway.py` + `bridge/trade_sync.py`) wurde während der UAT entdeckt und gefixt: Bot-Trades wurden fälschlicherweise als `bridge/tradeexecuter` attributiert statt dem Bot-ID. Fix committed als 22610ed. Bridge + TestBot 1 müssen neugestartet werden, damit die neue Ticket-Registry greift.
</current_state>

<completed_work>
**Plan 01 — Daten-Layer (vollständig abgeschlossen):**
- Task 1: Typ-Verträge in bot.ts (BotCommandType, BotStatus.parameters, SetParametersPayload, BotStats) — Commit 0607907
- Task 2: Stats-Endpunkt GET /api/bots/[id]/stats — Commit 9b5e875
- Task 3: Command-Route um set_parameters erweitert — Commit bfbed98
- SUMMARY.md erstellt — Commit 79fcb14

**Plan 02 — Bot-Karte (Code fertig, UAT ausstehend):**
- Task 1: Stats-Polling + Stat-Komponente mit valueColor-Prop — Commit 9f7084d
- Task 2: Stats-Grid auf P&L/Positionen/Trades/Uptime umgebaut — Commit 408d42f
- STATE.md aktualisiert — Commit 8a9ea0b
- Task 3 (UAT-Checkpoint): **OFFEN** — wartet auf Browser-Verifikation

**Plan 03 — Settings-Seite:** Noch nicht gestartet.

**Bridge-Fix:** ticket→bot Attribution in gateway.py + trade_sync.py — Commit 22610ed
</completed_work>

<remaining_work>
**Plan 02 — Task 3: UAT-Checkpoint (nächste Aktion)**
Bridge + TestBot 1 neustarten, dann im Browser auf http://localhost:3000/bots prüfen:
1. BOTS-01: "Positionen" zeigt echte Anzahl offener Trades (nicht 0)
2. BOTS-03: "P&L" statt "Balance"; grün/rot/`-`
3. BOTS-04: "Trades" Kachel mit Gesamtanzahl
4. BOTS-02: Kein "Synced"-Feld
5. BOTS-05: Bot stoppen → Karte verschwindet nach ~30s
Dann "approved" eingeben → Continuation-Agent für Plan 02 spawnen → SUMMARY.md

**Plan 03 — alle Tasks:**
- Task 1: Edit/Delete-Logik + UI aus BotsSettingsClient.tsx entfernen
- Task 2: Parameter-Editor mit Typ-Inferenz + Pro-Bot-Senden-Button
- Task 3: UAT-Checkpoint — visuelle Verifikation in Browser
- SUMMARY.md

**Nach Phase 03:**
- Code Review (`/gsd-code-review 3`)
- Verifikation (`gsd-verifier`)
- Phase abschließen
</remaining_work>

<decisions_made>
- Bridge-Bug-Fix direkt in Bridge-Python-Code (gateway.py + trade_sync.py) statt AlphaTrack-Server-Workaround — sauberere Attribution an der Quelle
- Reconnect-Logik in gateway.py: wenn Bot `bot_id` in config.json setzt, wird diese jetzt respektiert (statt immer alte UUID zu verwenden)
- Bot-Karte: `var(--red)` für negativen P&L (nicht inline `#ef4444`) gemäß UI-SPEC
- Stats-Polling-Interval: 8s mit Promise.allSettled für alle Bots
- Stats-Endpunkt: `realizedPnl = null` (nicht 0) wenn keine closed Trades — zeigt `-` statt `0.00`
</decisions_made>

<blockers>
- **UAT BOTS-05 (Karte verschwindet nicht):** Wurde während UAT gemeldet — möglicherweise Timeout-Schwellwert in BotStatusContext zu groß, oder finaler Heartbeat mit `state="stopped"` kommt nicht an. Needs investigation if still failing after Bridge restart.
- **Bridge muss neugestartet werden** damit ticket-Registry aktiv wird (in-memory, nicht persistent)
</blockers>

## Required Reading (in order)
1. `.planning/phases/03-bot-verbesserungen/03-02-PLAN.md` — Plan 02 Checkpoint-Details (Task 3: how-to-verify)
2. `.planning/phases/03-bot-verbesserungen/03-03-PLAN.md` — Plan 03 vollständig
3. `.planning/phases/03-bot-verbesserungen/03-01-SUMMARY.md` — was Plan 01 gebaut hat (Typen, Endpunkte)
4. `.planning/phases/03-bot-verbesserungen/03-UI-SPEC.md` — Design-Kontrakt für Pläne 02+03
5. `.planning/phases/03-bot-verbesserungen/03-PATTERNS.md` — Implementierungs-Snippets

## Infrastructure State
- **Bridge**: muss neugestartet werden (Ticket-Registry ist in-memory, greift erst nach Neustart)
- **TestBot 1**: muss neugestartet werden (damit neue bot_id aus config.json aktiv wird)
- **AlphaTrack Dev Server**: `npm run dev` auf Port 3000
- **Bot config**: `bots/testbot1/config.json` — `bot_id: "qwzDxCfKpp"` bereits korrekt gesetzt

## Infrastructure State — Uncommitted Files
Die folgenden Dateien sind modifiziert aber nicht committed (runtime-Daten, kein Handlungsbedarf):
- `data/*.json` — Live-Trade/Bot-Daten vom laufenden System
- `bridge/bridge_log.json`, `bridge/config.json` — Bridge-Runtime-Logs
- `bots/testbot1/config.json` — bot_id manuell auf qwzDxCfKpp gesetzt (bereits korrekt)

<context>
UAT für Plan 02 war halb durch als der Trade-Attribution-Bug entdeckt wurde. Stats-Kacheln zeigten 0 Trades obwohl der Bot lief. Ursache: Bridge-Sync setzt bot_id=None für MT5-Trades → normalizeTrade fallback → "bridge/tradeexecuter". Fix in gateway.py/trade_sync.py committed. Nach Bridge+Bot-Neustart sollte UAT Plan 02 retried werden. Plan 03 noch gar nicht angefasst.

Der "Karte verschwindet nicht"-Bug (BOTS-05) wurde auch gemeldet — noch nicht vollständig untersucht. Könnte nach Bridge-Neustart auch anders verhalten.
</context>

<next_action>
1. Bridge neustarten (auf Raspberry Pi / Host wo Bridge läuft)
2. TestBot 1 neustarten
3. `npm run dev` falls nicht läuft
4. Browser auf http://localhost:3000/bots
5. UAT Plan 02 Task 3 prüfen (BOTS-01/02/03/04/05)
6. Bei Erfolg: "approved" eingeben → Continuation-Agent für Plan 02 (SUMMARY.md + Plan 03 starten)
</next_action>
