---
phase: 04-performance-abschluss
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - bridge/gateway.py
autonomous: true
requirements:
  - PERF-01
  - PERF-02

must_haves:
  truths:
    - "Nach Bridge-Neustart sind alle zuvor erfassten Ticket→Bot-Zuordnungen wieder verfügbar"
    - "Neue Trades von Bots erhalten eine korrekte botId, sobald trade_sync.py läuft"
    - "Die JSON-Datei ticket_registry.json existiert im bridge/-Verzeichnis nach dem ersten Trade"
  artifacts:
    - path: "bridge/gateway.py"
      provides: "Persistierte _ticket_to_at_bot_id Registry mit Load-on-startup und Save-on-write"
      contains: "_TICKET_REGISTRY_FILE"
    - path: "bridge/ticket_registry.json"
      provides: "Persistierte Ticket→AlphaTrack-Bot-ID-Map (wird zur Laufzeit erzeugt)"
  key_links:
    - from: "bridge/gateway.py (_ticket_to_at_bot_id)"
      to: "bridge/trade_sync.py (get_at_bot_id_for_ticket)"
      via: "get_at_bot_id_for_ticket() liest aus dem im RAM gehaltenen Dict"
      pattern: "get_at_bot_id_for_ticket"
---

<objective>
Persistiert die In-Memory-Registry `_ticket_to_at_bot_id` in `bridge/gateway.py` als JSON-Datei
(`bridge/ticket_registry.json`), damit die Ticket→Bot-Zuordnung Bridge-Neustarts überlebt.

Purpose: Ohne Persistenz haben alle Trades `botId: null`, sobald die Bridge neu gestartet wird.
Die Performance-Ansicht und BotPerfCard zeigen deshalb für jeden Bot 0 Trades. D-01/D-02 (PERF-01).

Output: `bridge/gateway.py` mit drei Ergänzungen — Datei-Konstante, `_load_ticket_registry()`,
`_save_ticket_registry()` — sowie Aufrufen an den richtigen Stellen. `BotPerformanceClient.tsx`
und `BotPerfCard.tsx` bleiben unverändert (D-03).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-performance-abschluss/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Registry-Persistenz in gateway.py implementieren (D-01, per PERF-01)</name>
  <files>bridge/gateway.py</files>

  <read_first>
    Lies bridge/gateway.py Zeilen 182–196 (Registry-Deklaration + get_at_bot_id_for_ticket) und
    Zeilen 60–66 (configure-Funktion) und Zeilen 530–536 (close_position-Branch: pop aus Registry)
    und Zeilen 561–565 (execute_trade-Branch: Eintrag in Registry schreiben).
    Lies bridge/trade_sync.py vollständig — er ruft get_at_bot_id_for_ticket() auf.
  </read_first>

  <action>
    Ergänze bridge/gateway.py an vier Stellen. Keine anderen Zeilen ändern.

    STELLE 1 — Konstante (nach dem os-Import, vor config_lock-Zeile, ca. Zeile 19):
    Füge ein: _TICKET_REGISTRY_FILE = os.path.join(os.path.dirname(__file__), "ticket_registry.json")

    STELLE 2 — Hilfsfunktionen (direkt nach der _ticket_lock-Zeile, ca. Zeile 189, vor get_at_bot_id_for_ticket):
    Füge zwei Funktionen ein:

    def _load_ticket_registry() -> None:
        global _ticket_to_at_bot_id
        try:
            with open(_TICKET_REGISTRY_FILE, "r", encoding="utf-8") as f:
                raw = json.load(f)
            with _ticket_lock:
                _ticket_to_at_bot_id = {int(k): v for k, v in raw.items()}
        except FileNotFoundError:
            pass
        except Exception as e:
            print(f"[gateway] Ticket-Registry laden fehlgeschlagen: {e}")

    def _save_ticket_registry() -> None:
        try:
            with _ticket_lock:
                snapshot = dict(_ticket_to_at_bot_id)
            with open(_TICKET_REGISTRY_FILE, "w", encoding="utf-8") as f:
                json.dump({str(k): v for k, v in snapshot.items()}, f)
        except Exception as e:
            print(f"[gateway] Ticket-Registry speichern fehlgeschlagen: {e}")

    STELLE 3 — Laden beim Start (am Ende der configure()-Funktion, nach configure_udp-Zeile):
    Füge ein: _load_ticket_registry()

    STELLE 4a — Speichern nach Eintrag (execute_trade-Branch, direkt nach der Zeile
    `_ticket_to_at_bot_id[int(result["ticket"])] = at_id`):
    Füge ein: _save_ticket_registry()

    STELLE 4b — Speichern nach Löschung (close_position-Branch, direkt nach der Zeile
    `_ticket_to_at_bot_id.pop(int(close_ticket), None)`):
    Füge ein: _save_ticket_registry()

    Hinweis: _save_ticket_registry() liest das Dict unter _ticket_lock; der Lock ist zu diesem
    Zeitpunkt bereits freigegeben (pop/write erfolgte innerhalb des with-Blocks davor) —
    daher keinen verschachtelten Lock-Aufruf erzeugen.
  </action>

  <verify>
    <automated>cd "c:\Users\Kevin\Desktop\AlphaTrack\bridge" && python -c "import gateway; print('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - `python -c "import gateway; print('OK')"` gibt "OK" aus ohne Traceback
    - Die Zeichenkette "_TICKET_REGISTRY_FILE" erscheint in bridge/gateway.py
    - Die Zeichenkette "_load_ticket_registry" erscheint in bridge/gateway.py
    - Die Zeichenkette "_save_ticket_registry" erscheint in bridge/gateway.py
    - "_load_ticket_registry()" steht im Body von configure() (nach configure_udp-Aufruf)
    - "_save_ticket_registry()" steht einmal im execute_trade-Branch und einmal im close_position-Branch
    - Keine anderen Funktionen oder Variablen wurden entfernt oder umbenannt
  </acceptance_criteria>

  <done>
    gateway.py importiert sauber, alle fünf Einfügungen sind vorhanden, keine Syntax-Fehler.
    Nach dem nächsten Bridge-Start wird ticket_registry.json erzeugt sobald ein Bot einen Trade
    öffnet. trade_sync.py findet die Zuordnung auch nach Bridge-Neustart über get_at_bot_id_for_ticket().
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| Dateisystem → gateway.py | ticket_registry.json wird aus dem Bridge-Verzeichnis gelesen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | ticket_registry.json | accept | Lokale Datei, kein Netzwerkzugriff; enthält nur int→string Mappings ohne Credentials |
| T-04-02 | Denial of Service | _save_ticket_registry() | accept | try/except fängt alle Fehler; schlechtester Fall ist Datenverlust der Registry, nicht Absturz |
</threat_model>

<verification>
Manuelle Verifikation nach Implementierung:
1. Bridge starten, einen execute_trade-Command von einem Bot ausführen lassen
2. Prüfen: bridge/ticket_registry.json existiert und enthält `{"<ticket>": "<at_bot_id>"}`
3. Bridge neu starten
4. trade_sync.py läuft durch: Ticket wird über get_at_bot_id_for_ticket() aufgelöst → bot_id gesetzt
5. Frontend-Performance-Ansicht zeigt Trades für den Bot (statt 0)
</verification>

<success_criteria>
- bridge/gateway.py importiert ohne Fehler
- _TICKET_REGISTRY_FILE, _load_ticket_registry, _save_ticket_registry sind definiert
- configure() ruft _load_ticket_registry() auf
- execute_trade-Branch ruft _save_ticket_registry() nach dem Schreiben des Eintrags auf
- close_position-Branch ruft _save_ticket_registry() nach dem Löschen des Eintrags auf
- PERF-01 und PERF-02 sind damit datenseitig erfüllt (Frontend-Filter in BotPerformanceClient.tsx
  war bereits korrekt per D-03, BotPerfCard.tsx zeigt Trades korrekt sobald botId gesetzt ist)
</success_criteria>

<output>
Erstelle `.planning/phases/04-performance-abschluss/04-01-SUMMARY.md` wenn abgeschlossen.
</output>

## Artifacts this phase produces

| Symbol | Datei | Art |
|--------|-------|-----|
| `_TICKET_REGISTRY_FILE` | `bridge/gateway.py` | Neue Konstante |
| `_load_ticket_registry()` | `bridge/gateway.py` | Neue Funktion |
| `_save_ticket_registry()` | `bridge/gateway.py` | Neue Funktion |
| `configure()` | `bridge/gateway.py` | Geändert: ruft _load_ticket_registry() auf |
| execute_trade-Branch | `bridge/gateway.py` | Geändert: ruft _save_ticket_registry() auf |
| close_position-Branch | `bridge/gateway.py` | Geändert: ruft _save_ticket_registry() auf |
| `bridge/ticket_registry.json` | (zur Laufzeit erzeugt) | Neue Datei |
