---
phase: 04-performance-abschluss
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/bots/logs/page.tsx
  - src/app/bots/logs/BotsLogsClient.tsx
  - src/components/layout/Sidebar.tsx
autonomous: true
requirements:
  - BOTLOG-01

must_haves:
  truths:
    - "Der Sidebar-Eintrag 'Bot Log' ist nicht mehr sichtbar"
    - "Navigieren zu /bots/logs ergibt 404 (Seite existiert nicht)"
    - "Die restliche Bot-Navigation (Bots, Bot Settings, Strategien, Performance) funktioniert unverändert"
  artifacts:
    - path: "src/components/layout/Sidebar.tsx"
      provides: "BOTS_NAV ohne { href: '/bots/logs', label: 'Bot Log', icon: ScrollText }-Eintrag"
  key_links:
    - from: "src/components/layout/Sidebar.tsx (BOTS_NAV)"
      to: "/bots/logs"
      via: "Link-Komponente"
      pattern: "bots/logs"
---

<objective>
Entfernt die veraltete Bot-Log-Seite (`/bots/logs`) vollständig: Zwei Dateien werden gelöscht,
ein Sidebar-Eintrag wird aus BOTS_NAV entfernt (D-05, D-06, BOTLOG-01).

Purpose: Die Seite ist nicht mehr Teil des Produkt-Scopes; ein toter Link in der Sidebar
erzeugt Verwirrung und eine unnötige Route.

Output: Keine `/bots/logs`-Dateien mehr im Repo, kein Sidebar-Link mehr in BOTS_NAV.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-performance-abschluss/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bot-Log-Dateien löschen (D-05, per BOTLOG-01)</name>
  <files>src/app/bots/logs/page.tsx, src/app/bots/logs/BotsLogsClient.tsx</files>

  <read_first>
    Lies src/app/bots/logs/page.tsx vollständig (kurze Datei, Server Component).
    Lies src/app/bots/logs/BotsLogsClient.tsx vollständig.
    Prüfe: Werden diese Dateien irgendwo anders importiert?
      grep -r "bots/logs" src/ --include="*.tsx" --include="*.ts" (außer Sidebar und den Dateien selbst)
    Falls ein anderer Import existiert: diesen ebenfalls entfernen, bevor die Dateien gelöscht werden.
  </read_first>

  <action>
    Lösche beide Dateien:
    - src/app/bots/logs/page.tsx
    - src/app/bots/logs/BotsLogsClient.tsx

    Falls das Verzeichnis src/app/bots/logs/ danach leer ist, kann es ebenfalls entfernt werden
    (Next.js benötigt kein leeres Verzeichnis).

    Keine anderen Dateien in src/app/bots/ anfassen.
  </action>

  <verify>
    <automated>powershell -Command "if (Test-Path 'src/app/bots/logs/page.tsx') { exit 1 } else { Write-Output 'OK' }"</automated>
  </verify>

  <acceptance_criteria>
    - src/app/bots/logs/page.tsx existiert nicht mehr im Dateisystem
    - src/app/bots/logs/BotsLogsClient.tsx existiert nicht mehr im Dateisystem
    - Kein anderer TypeScript-/TSX-File im src/-Verzeichnis importiert diese Pfade
    - `npm run build` läuft ohne "Module not found"-Fehler für bots/logs
  </acceptance_criteria>

  <done>
    Beide Dateien sind gelöscht. Next.js wird bei Aufruf von /bots/logs automatisch 404 zurückgeben.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sidebar-Eintrag 'Bot Log' entfernen (D-06, per BOTLOG-01)</name>
  <files>src/components/layout/Sidebar.tsx</files>

  <read_first>
    Lies src/components/layout/Sidebar.tsx Zeilen 36–42 (BOTS_NAV-Array).
    Der zu entfernende Eintrag ist:
      { href: '/bots/logs', label: 'Bot Log', icon: ScrollText },
    Er steht in der BOTS_NAV-Konstante an Zeile 38 (laut CONTEXT.md).
    Prüfe nach dem Entfernen: Wird ScrollText noch anderswo in der Datei genutzt?
    Wenn ScrollText nur für diesen Eintrag importiert war → Import-Zeile ebenfalls anpassen
    (ScrollText aus dem lucide-react-Import entfernen), um TS-Compiler-Warnungen zu vermeiden.
    BRIDGE_NAV hat einen eigenen ScrollText-Eintrag (Bridge Log) — prüfe ob der Import
    weiterhin benötigt wird.
  </read_first>

  <action>
    Entferne aus BOTS_NAV (Zeile ~38) exakt diese Zeile:
      { href: '/bots/logs', label: 'Bot Log', icon: ScrollText },

    Prüfe danach, ob ScrollText noch in BRIDGE_NAV oder einer anderen Stelle in Sidebar.tsx
    verwendet wird (Zeile ~33: BRIDGE_NAV enthält { href: '/bridge/log', label: 'Bridge Log', icon: ScrollText }).
    Wenn ScrollText noch durch BRIDGE_NAV genutzt wird → Import bleibt. Nur wenn ScrollText
    nach dem Entfernen nirgends mehr vorkommt → aus dem lucide-react-Importblock entfernen.

    Keine anderen BOTS_NAV-Einträge oder Sidebar-Logik ändern.
  </action>

  <verify>
    <automated>powershell -Command "if (Select-String -Path 'src/components/layout/Sidebar.tsx' -Pattern \"bots/logs\" -Quiet) { exit 1 } else { Write-Output 'OK' }"</automated>
  </verify>

  <acceptance_criteria>
    - Die Zeichenkette "/bots/logs" kommt in src/components/layout/Sidebar.tsx nicht mehr vor
    - BOTS_NAV enthält weiterhin: /bots, /bots/settings, /strategien, /bots/performance
    - BRIDGE_NAV bleibt unverändert (inklusive Bridge Log)
    - Kein ungenutzter Import in Sidebar.tsx (kein TypeScript-Fehler "is declared but never read")
    - `npm run build` kompiliert ohne TypeScript-Fehler
  </acceptance_criteria>

  <done>
    Sidebar zeigt keinen "Bot Log"-Link mehr. Build läuft durch.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| Keine neuen Trust Boundaries | Nur Dateilöschung und Sidebar-Anpassung |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Tampering | Keine | accept | Reine Entfernung; keine neuen Angriffspunkte eingeführt |
</threat_model>

<verification>
Nach beiden Tasks:
1. `npm run build` (PowerShell: `npm run build`) — muss ohne Fehler durchlaufen
2. Browser: /bots/logs aufrufen → 404
3. Sidebar: "Bot Log"-Eintrag unter Bots-Navigation nicht mehr sichtbar
4. Sidebar: "Bridge Log" unter Bridge-Navigation weiterhin vorhanden
</verification>

<success_criteria>
- page.tsx und BotsLogsClient.tsx gelöscht
- Sidebar enthält keinen /bots/logs-Link mehr
- npm run build ohne Fehler
- BOTLOG-01 erfüllt
</success_criteria>

<output>
Erstelle `.planning/phases/04-performance-abschluss/04-02-SUMMARY.md` wenn abgeschlossen.
</output>

## Artifacts this phase produces

| Symbol | Datei | Art |
|--------|-------|-----|
| *(gelöscht)* | `src/app/bots/logs/page.tsx` | Gelöscht |
| *(gelöscht)* | `src/app/bots/logs/BotsLogsClient.tsx` | Gelöscht |
| `BOTS_NAV` | `src/components/layout/Sidebar.tsx` | Geändert: Bot-Log-Eintrag entfernt |
